export const version = '1.48.0';

export const CHANGELOG = [
  {
    version: '1.48.0',
    date: '2026-08-19',
    changes: [
      'THE THREE HANDS TAKE A REGION NOW. `HANDS.fill(kind, w, h, seed, region, opt)` — same jitter, same drift, same overshoot, same re-grip, same four thresholds each confined to what the last left dark, with the SUBJECT taken out. They were the only generators in the file that could not be pointed at one, which cost nothing until there was a third subject and three tiles of nine quietly drew something else',
      'THE WRAPPING PASS IS `streamlines`, AND IT HAD TO BE. `latitudes` wraps a SPHERE: it wants a centre, a radius and an axis, and its one front-facing pole has to be aimed at the highlight or the fill grows a whirlpool. A teapot has no such axis and no honest place to put one. Streamlines over the field the tone itself implies is the same idea arrived at from the other end — it works on any region and has no pole to place',
      'HOW MANY PASSES IS ASKED OF THE TONE. Four passes each confined to what the last left dark is how you build a tone; hand that to a FLAT region — the blob is 1 everywhere inside — and all four thresholds pass at once, four interleaved passes at pitch s land at s/4, under two nibs, and it floods to solid black. The region is sampled once for its range and a flat one gets two passes, which is what `drawShape` has always done and never said why',
      'AND EACH WRAPPING PASS IS SPACED AN ELEVENTH WIDER THAN THE LAST. Four runs of the same field at the same separation land on the same lines, and since every pass is confined to what is already darker they would stack exactly on top of each other in the shadow — inking it twice instead of building it up',
      '`solid()` HANDS BACK `rings` AS WELL AS `edge`. A hand inks a contour as a run of separate marks with gaps in it and cannot do that to a path string. Same marching-squares segments, linked head to tail — and because a cell emits its ends on a shared lattice, two segments meet when their endpoints round to the same point and no tolerance is needed. Verified: no jump anywhere in the chain, and the handle hole comes back as its own ring the moment the view opens it',
      'DRAW AND DRAWSHAPE ARE UNTOUCHED. Portrait-Typo calls both, and changing what they return would change a drawing in somebody else’s app to tidy this one. The amber caveat on three tiles is gone because it is no longer true',
    ],
  },
  {
    version: '1.46.0',
    date: '2026-08-19',
    changes: [
      'THE ROW PICKS THE MODEL — SHAPE, SPHERE, TEAPOT. It was STROKE / HATCH / SHADOW with two of the three dead, which is a promise on a page that already has enough of them. Three live keys beat three keys of which one works. And the subject is now chosen in exactly ONE place: it was in the bay and again over the grid, which is two controls on one key — they do not sync each other, so they drift apart until something rebuilds them both. The bay owns it, the grid follows',
      'ONE PANE, AND NO RASTER ANYWHERE. There were two: the strokes, and a grayscale render of the tone beside them. The second was the only raster on a page whose entire claim is that what you see is what the pen puts down, and having it there left the honest half competing for the eye with a picture of the thing it was an approximation OF. One pane, all strokes, no exception to explain',
      'THE DRAG DRAWS THE SILHOUETTE AND THE FILL LANDS WHEN YOU LET GO. Re-rendering the solid turns at a usable rate; re-running the fill does not — five hundred milliseconds on the labyrinth at the reference pitch and two and a half SECONDS at the tight end of the spacing knob, with the thread inside the generator and nothing painting, including whatever would have said why. Posing the object and then plotting it is also the honest order to do it in',
      'AND THE DRAG RENDERS AT HALF THE SAMPLING. `solid`’s cost goes with the square of `res`, so at the bay’s sheet a full-detail render is fifty-five milliseconds — eighteen frames a second, which feels like treacle. At 1.8 it is twenty-five and it is sixty. Nothing is lost: what the drag draws is an annotation saying which way round the model is, and the fill that follows is computed from a fresh full-detail render',
      'THE SHEET WANTS A SHEET’S PROPORTION. 820 wide by 300 tall was a 206 × 79mm strip, two and a half to one, and a teapot fitted into that is a small object marooned in a lot of paper. 760 × 430 is 189 × 114mm and the object fills it',
      'OPEN IN BAY IS GONE. With the type list in the bay and the model row beside it there was nothing left for it to do that a glance at the list does not, and a key per tile for that is nine keys earning one press between them',
    ],
  },
  {
    version: '1.43.0',
    date: '2026-08-19',
    changes: [
      'A HATCH BAY, AND THE GRID FEEDS IT. Two questions needed two answers: the grid is which fill do I want — nine at once, comparable, scanned once — and the bay is what does this one DO when I turn it. A paint chart and a mixing palette. Every tile now carries an OPEN IN BAY key. The grid stays pinned at the reference setting and only the bay deviates, because nine tiles that each drifted with the knobs would stop being a comparison the moment you touched anything',
      'THE GRID WAS LYING BY OMISSION. Each of those nine is a FAMILY, not a drawing, and a page of nine fixed configurations claims otherwise. Pitch measured in nibs is the most important number in the engine — the API says so in bold twice — and until now there was nowhere on the page to move it. All nine generators now take the same nib, spacing and angle and derive their own numbers from them, so ONE spacing knob means something on all of them, hands included',
      'DRAG TURNS THE OBJECT, IT DOES NOT MOVE IT. Sliding a teapot across the sheet teaches nothing — same fill, different place. Turning it changes which surfaces face the lamp and which face the camera, which is the whole of what a fill has to cope with and the one thing a still specimen cannot show',
      'THE TONE PANE FOLLOWS THE HAND AND THE PLOT PANE WAITS. Re-rendering the solid is thirty milliseconds; re-running the fill is five hundred on the labyrinth and two and a half SECONDS at the tight end of the spacing knob. A plot that tried to keep up would not be slow, it would be dead — the thread is in the generator and nothing paints, including whatever would have told you why. So the plot is coalesced ninety milliseconds after you stop, and the stale state is announced BEFORE the work starts and across two frames: one to apply the class, one to let the browser paint it. A busy flag set on the same tick as a blocking loop is a flag nobody ever sees',
      'THE LAYER ROW IS BUILT AND DIMMED. STROKE and SHADOW are not wired; HATCH is. They are there because the shape of the thing is part of what the page documents — a plot is LAYERS and each takes its own fill — and because `solid()` already hands back exactly those three, `edge`, `tone` and `shadow`, so the model is waiting on the wiring rather than the other way round. A row that appears later is a surprise; a row visibly unfinished is a promise',
      'AND ONE RASTER, LABELLED. Everything on this page is a stroke at a pen width because that is the claim; the tone pane is the thing those strokes approximate, and drawing it as strokes would answer the question with the answer. It is captioned `tone`, it is never plotted, and it is the pane you drag — so what follows your hand is the model and what waits is the plot',
    ],
  },
  {
    version: '1.41.0',
    date: '2026-08-19',
    changes: [
      'THE BALL IS A REAL SOLID NOW, and the reason is not tidiness. `scene()` lit it one way and `teapot()` lit its object another, so switching subject changed the SHAPE and the TONE MODEL at once — and a grid whose only job is comparing fills cannot vary two things at a time: you could not tell whether a fill behaved differently because the subject curved differently or because its tone had been made a different way. Two lighting models in one file is exactly the drift KIT exists to prevent',
      '`solid(w, h, mesh, opt)` IS THE RENDERER AND IT DOES NOT KNOW WHAT A TEAPOT IS. It was written as `teapot()`, which was one subject’s worth of thinking. It takes a triangle mesh with normals and returns tone, shadow and edge; the mesh belongs to the caller and the LIGHTING lives here, once. `KIT.teapot` and `KIT.ball3d` are two lines each. The sphere sits on the table at the same height the pot’s foot does, so both get the same contact, the same bounce and the same projected shadow',
      'THE CACHE KEY NAMES THE MESH. It did not, so the second solid would have been served the first one’s render — the same class of bug as the lamp missing from the key one version ago, and the second time it has bitten in this function',
      'THE SPHERE’S DIAGONAL IS THE TERMINATOR AND WAS NEVER A BUG. Two changes went in hunting it — a slope-scaled shadow bias and a smooth bounce falloff — before measuring showed the lamp sits 101° off the camera, which means the terminator great circle is seen nearly edge-on and projects to something very close to a straight line. Correct, deliberate, and the best thing on the specimen: a hard tonal turn for a hatch to cross. Both changes stay because both are right on their own terms, and both comments were rewritten to say so — a comment that claims credit for a fix that never happened is worse than no comment',
    ],
  },
  {
    version: '1.39.0',
    date: '2026-08-19',
    changes: [
      'A TEAPOT, AND IT IS ACTUALLY RENDERED. `scene()`’s ball is a DRAWING of a lit sphere — an analytic function of paper position with a fake normal, hand-tuned until it reads — which is the right way to make a specimen and the wrong way to make a test, because a function of (x, y) has no inside, nothing occludes anything, and the cast shadow is an ellipse somebody chose. `KIT.teapot(w, h, opt)` tessellates 32 bicubic Bézier patches, rasterises depth and normal from an orthographic camera and depth from the light, and hands back tone, shadow and edge. Four cases nothing else on the page could reach: self-occlusion where the spout crosses the body, a hole that is not the outline, saddle curvature at the neck, and a cast shadow that is projected rather than drawn',
      'AND IT IS STILL JUST `tone(x, y)`. Not one line of any fill changed. The renderer runs once into buffers and the tone function is a lookup returning -1 outside the silhouette — which is the contract doing exactly what it is for, and the whole argument for having had one',
      'THE PATCH TABLE IS READ TRANSPOSED, and it is not a tidy-up. Row-major gives an identical surface — a Bézier patch is symmetric under transposing its net and swapping u for v — but du × dv comes out the other way and EVERY NORMAL POINTS INTO THE POT. It renders, it shades, and it is lit from the wrong side by 180°. Measured rather than reasoned: outward is 42 of 48 sampled body points this way, 6 of 48 the other, and the six are the lid underside, which faces in and should',
      'SIXTEEN DEGREES OF ELEVATION, NOT TWENTY-SIX. Far enough above and the spout foreshortens into the body, the handle merges with the rim, and the silhouette — the thing every fill has to fill — is a rounded lump with a nub on it. The lamp was picked the same way, against three others: near the vertical the lid lights and every side of the body falls to one middling dark, with no terminator anywhere, which is the one feature a fill is here to render',
      'THE CACHE KEY IS EVERY INPUT. It was size, segments and angles — not the light — so a comparison of four lamp positions came back as four copies of the first render with four identical tone ranges printed under them. The sort of wrong that looks like a finding',
      'A DIRECTIONAL SHADOW DOES NOT GET LIGHTER BECAUSE THE CASTER IS TALL. Grading it by the blocker’s height drew a pale disc inside a dark one — the body’s shadow lighter than the foot’s, a ring nobody would draw. One tone, darkened at the contact, and the contact measured off the mask rather than assumed',
      'THE THREE HANDS SAY THEY ARE NOT ANSWERING. `HANDS.draw`/`drawShape` predate the region contract: they build their own subject and cannot take one, so on any subject but the ball those three tiles are drawing something else. Marked in amber on the tile rather than left to pass silently. Fixing it properly is a real piece of work on HANDS — both entry points, its four thresholds, and `latitudes`, whose axis is sphere-specific — and it is filed rather than fudged',
    ],
  },
  {
    version: '1.35.0',
    date: '2026-08-18',
    changes: [
      'FOUR MORE MACHINE FILLS, and they are generators rather than settings. HATCH.streamlines is Jobard & Lefer evenly-spaced placement over a direction field derived from the tone itself — the only fill here where tone is the SEPARATION and not a threshold, so it has no banding to dither away. HATCH.squiggle is Ahmed & Deussen amplitude modulation: nothing is ever broken, so a whole region stitches down to one stroke. HATCH.spaceFill is a Hilbert curve that recurses a level deeper where it is darker. HATCH.labyrinth is Pedersen & Singh growth, one closed curve grown until the region is full. All four take a region and hand back runs, so stitch and cut work on them exactly as on scanlines',
      'THE DIRECTION FIELD IS BENT, NOT BLENDED, and both halves of that are bugs if undone. Blending a uniform field with a rotational one always leaves a point where the two cancel and every streamline near it spirals in — a whirlpool with no warning, the same failure as an unplaced latitudes pole. And the bend is sin(2d) because the iso-tone direction is a LINE, not an arrow: fold it into a half-turn range instead and there is a seam where the fold happens, which draws as a staircase straight across the region',
      'STITCH IS BOUNDED WHEN THE BRIDGE SAYS SO. cut now publishes its own reach, and stitch buckets the free ends into a grid sized to it instead of walking every end on every step. Not an approximation of the greedy rule — cut refuses every gap wider than reach, so a candidate outside the neighbourhood could never have been taken. A bridge that publishes no reach gets the exhaustive search unchanged. O(n³) to roughly O(n) over a fill, which is what makes a ten-thousand-run streamline fill finish',
      'ENG HAD FOUR MARKS TWICE. hhand, hcross, serp and hrand were each defined twice in the same object literal, the second silently overwriting the first — left behind by the v1.34.1 merge that brought them into the canonical table. Deduplicated, and four new marks added for the fills above',
      'ONE FRAME, NOT THREE. A specimen arrived inside a `box` inside a `stage`: the box put a lit head bar, a bevel and a 42px drop shadow around a thing whose whole job is to be a flat sheet under glass, and its head said what the stage-note already said. Its fixed 318px is also what made section 05 break out of the column. The stage is the panel now — it keeps its note, takes the section’s one key up beside it, and the cells sit on it directly. Stage, screen, paper. The screen is untouched; it is the part that makes a white sheet read as being under glass rather than as a hole in the page',
      'THE CALL SITS BESIDE THE DRAWING, and it is not a panel either. Under the sheet it was a one-line strip the width of the specimen — a fourth surface, and the smallest thing on the page. Beside it there is room for the whole call: the region test, the generator with the options that specimen really used, and the stitch that produced the pen-down number in the caption. A hairline rule says which drawing it belongs to, which is what a rule is for. It falls under the sheet again below 760px, where there is no beside',
      'SECTION 05 IS TWO UP on `auto-fit`, so the fall to one column happens when the cell stops fitting rather than at a width someone guessed. The API list is grouped under KIT / HANDS / HATCH — the three names §02 already teaches, rather than a second vocabulary for the same thing — and the group heading takes its empty dd OUT of the grid: a heading spanning both columns followed by an empty cell puts the next TERM in column two and every row after it is off by one. Nothing throws, the list quietly stops lining up',
      'THE SPECIMEN SHEETS ARE WHITE. They had the bay’s toned #BEB5A3 on the grounds that the two should match, which was never the bay’s actual reason — the bay has a WHITE pen in its case, and white on cream is no result at all. This block has one pen and it is black, so the ground can be the one these fills are for. Black on white is 18.4:1 against 8.7:1, which at a 0.35 nib is the difference between reading a pitch and estimating it. The inner shadow drops from 8px at .4 to 6px at .16 — a well under a toned sheet is a grey vignette under a white one, dirtying the corners of the thing being judged — and the 1px rim, the reflection and the bezel are untouched. The bay’s own sheet stays toned; that constraint is real',
      'THE HATCH PAGE HAS A FIFTH SECTION and the API list has the five new names. Same two subjects as 08b, same pen, same conservative bridge, so the pen-down readouts compare directly with the serpentine’s. The flat shape is the half worth looking at: the streamline fill comes out as plain parallels there, because the field only bends where there is a tone to bend toward',
    ],
  },
  {
    version: '1.34.1',
    date: '2026-08-18',
    changes: [
      'FIX — the grid rig\'s body was `set-body gridbody`, missing `box-body`, which is the class that makes a panel body a padded column and carries the z-index that lifts it over `.box::before`. The same omission the segment rig had. Every body on this site is `box-body` plus its own class',
      'FIX — the kit\'s ENG never had the three hands or the serpentine. PANELS carries its own inline copy of the kit and had added those four marks to THAT, so the canonical table has been four short for as long as both copies existed. They are in src/skew-kit.js now and the HATCH page reads them off the kit like every other engraving, instead of assigning its own onto it',
      'FIX — `.intcell > .bay-cap` shipped into dist. It named `bay-cap`, which is a part, so the build kept the whole rule even though `.intcell` is doc-only. It is `.intcell-cap` now, a name the build knows to drop. 61 doc rules dropped instead of 60, and nothing doc-only is left in the shipped CSS',
    ],
  },
  {
    version: '1.34.0',
    date: '2026-08-18',
    changes: [
      'THE HATCH HEADER SAYS WHAT THE PAGE IS FOR. Everything that turns a region into pen strokes. Add yours if you have more. Fix the one you found a bug in. Three lines, and the last two are the invitation — this is the one part of Skew a second person is meant to add to, and the masthead had been spending five lines on where the engine used to live instead of saying so',
      'SECTION 06 IS CONTRIBUTING, not \'growing it\'. The repo path, the dev command, and every file with what it is for: the engine, this page, the bay that borrows the hands, the generated copy an app takes, and links to HATCH-API.md and CONTRIBUTING.md. Then a new hand as three places, because it is three and the third one is the hatch bay\'s dial',
      'FIVE BUGS FILED IN BUGS.md that had been living in a chat log: the HATCH specimens rendering empty, the grid rig missing box-body the same way the segment rig did, the kit\'s ENG and PANELS\' inline copy having drifted apart, .intcell > .bay-cap shipping into dist, and .seg still sitting dead in Portrait-Ribbons\' vendored CSS. Four of them were found while fixing something else and fixed nowhere. A fix is not a record, and the changelog is the wrong file for it: it is read after the fact, and BUGS.md is read before starting',
    ],
  },
  {
    version: '1.33.0',
    date: '2026-08-18',
    changes: [
      'THE COPY WAS CHANGELOG WEARING A LEDE. The HATCH masthead spent five lines on where the engine used to live and which version moved it, which is a thing a reader has no use for and version.js already says. It is one line now: everything that turns a region into pen strokes. Every masthead got the same cut, and so did the §01 rules, the §02 layer table and the §06 traps. Nothing was removed that says what something IS or what it costs to get wrong',
      'FIX — the HATCH page redeclared `eng` and `ENG`, both of which src/skew-kit.js already defines at top level. A second `const` of a name in the same script is a SyntaxError, so the page script never parsed and nothing on the page ran at all. The four hand engravings are Object.assign\'d onto the kit\'s table now, which is what they always should have been: the table already carries the plotter\'s marks',
      'AND THE KIT\'S ENG NEVER HAD THE HAND MARKS. PANELS carries its own inline copy of the kit and had added hhand, hcross, hrand and serp to that copy, so the canonical table and the inline one have been out of step for as long as both existed — SYSTEM §01 has a trap row about exactly this pair of copies and it was right',
      'tools/run-page.mjs RUNS A PAGE\'S SCRIPT UNDER A STUB DOM, which is the check that was missing. `node --check` is syntax only: it passes a page that throws a ReferenceError on load, and a page that throws kills every block after it while still rendering whatever ran first — so it reads as one broken section rather than as a broken page. The stub cannot lay anything out, and it is not meant to; what it catches is the free variable, the missing export and the call on undefined',
      'It is honest about what it cannot do. The four established pages still fail under it on stub gaps — Image, body.prepend, a subtree built from innerHTML — and those are the stub\'s limits, not the pages\'. A checker that claims more than it tests is worse than none',
    ],
  },
  {
    version: '1.32.0',
    date: '2026-08-18',
    changes: [
      'THE LABELLED SWITCH EXISTS. `toggle({label, value, icon, lamp, onChange})` — the switch, the name etched beside it, and the whole line as the button. It was the oldest open request in this file: `swBtn` puts the label in the `aria-label` and the `title` and never on screen, because it was written for a layer row where the ROW carries the name, and every adopter has since composed the same molecule out of the same three parts. Four apps composing one molecule four ways is how a language stops being one',
      'AND THE ICON DECIDES WHICH PART IT IS. A boolean with an honest glyph — an eye for a preview, an outline for the outlines — is the backlit cap, silkscreened, exactly as the layer dock builds it. Three of the nineteen booleans in the app this was written for had one. The other sixteen get a SLIDE SWITCH: a ribbed actuator in a slot milled through the faceplate, thrown left or right, where the legend is the POSITION and there is nothing printed on it at all. An arbitrary glyph is worse than none, and a blank cap is worse than both',
      'AND THERE IS A THIRD DRESS, `.slide.chrome` — THE SEALED CAPSULE. A and B are both cut INTO the panel; this one is bolted ONTO it, and that is most of why it reads as a bought component rather than as a graphic: a moulded bezel standing proud with light along its top and a cast under its bottom, a pale diffuser sunk inside it, a dark moulded paddle rocking in the track, and a pilot lamp under the diffuser\'s own surface. The track is pale in BOTH states, grey when dark and white when lit, which is what a translucent window over a lamp actually does — and it is also the loudest thing in the comparison on a near-black panel, which is the case against it',
      'AND C WAS DELETED AND STARTED AGAIN, TWICE, WHICH IS THE FINDING RATHER THAN THE ACCIDENT. Every version of it was ONE OBJECT IN A ROW — a capsule with something sliding in it, redrawn at three sizes and two materials — and none of that was the problem: a slide switch reads as a graphic because a slide switch IS mostly flat, and no amount of shading rescues a flat thing. The reference is not one object at all. It is a pilot LAMP, a SWITCH and an ENGRAVED NAME stacked in a column, which is how a console mounts one, and the ASSEMBLY is what carries the weight. Three channels, three parts, none of them repeating another: the lamp says what is happening, the paddle says what position it is in, the plate says what it is called',
      'AND THEN C WAS STOPPED AND REDONE IN SVG, because eight passes of stacked background gradients failed for one reason no ninth pass would have fixed: a CSS box is a RECTANGLE, and every gradient painted on it is an attempt at a form rather than the form. The tilt, the crease, the end wall and the way the sunk end loses its edge are all FACETS, and a facet has a boundary you either draw or approximate — approximating four of them on top of each other is how the part kept arriving as a light pill in a dark slot',
      'THE PADDLE IS A TRAPEZOID PATH: 24 units tall at the proud end with a moulding radius, 15 and nearly square at the sunk end because that end is further from the eye. The silhouette does the work, and a silhouette beats shading every time. The crease between the two is two cubic segments rather than a straight taper — a surface that turns away TURNS, and a straight taper is a wedge. The wall of the proud end is its own path with its own fill, because it is the one surface facing the lamp squarely. And the sunk end ends in the well\'s own black as a gradient STOP rather than as an overlay, so there is no edge to find',
      'THE TWO POSITIONS ARE MIRRORS OF ONE GEOMETRY, flipped about the switch\'s centre rather than drawn twice, so they cannot drift apart. The MARKS do not flip — they are printed on the panel\'s own left and right — and the one on the end that has turned away dims, because a surface out of the light does not show its legend either',
      'AND THE GRADIENT IDS ARE SUFFIXED PER INSTANCE. Three switches on a page is three copies of every gradient, and duplicate SVG ids resolve to the FIRST in the document — so all three would quietly share the first switch\'s paint and any per-layer colour would stop working. It lives in the page rather than in skew-kit.js because it is a candidate: two of the three dresses get deleted, and a part earns its way into the kit by winning',
      'THE SLIDE SWITCH IS FIVE SURFACES, AND THE FIRST TWO PASSES READ AS CHEAP FOR A SPECIFIC REASON. A small part looks cheap when its shading does not describe a SOLID: stripes instead of ridges, one flat fill instead of a chamfer and a face, nothing cast anywhere, and every highlight pure white — which is the tell, because a lamp has a colour and metal has a colour and their product never lands on #fff. The slot now builds a FLOOR and an ACTUATOR as children, and the actuator carries two more surfaces of its own: the knurl cut into its face and one anisotropic specular streak across the crowns',
      'FIX — THE TWO DOTS IN THE ACTUATOR\'S RIGHT-HAND CORNERS. An inset shadow with an offset and no blur cannot follow a radius: it is the shape pushed one pixel sideways, so at every rounded corner the offset copy pulls away from the edge and leaves a spot sitting in the turn. The chamfers are clipped background BANDS now — a background is clipped by the border-radius, so a 1px bright band across the top simply stops where the corner rolls away from the lamp, which is what a chamfer does — and every remaining inset carries a pixel of blur. The slot\'s own two edge lines had the same fault and are fixed with it',
      'AND THE METAL CAME DOWN A FULL STOP. It was reading as bright alloy against a black plate — the lightest object on the panel, which a small switch has no business being. The face is dull now and the broken edge carries the contrast, which is also how a real one works',
      'AND THE ACTUATOR IS WIDER THAN THE SLOT, which is the single biggest thing that stopped it reading as an object. A rectangle inside a rectangle is two rectangles; an actuator with a FLANGE — 24px in a 22px opening, overhanging a pixel top and bottom, casting onto the slot\'s own edge — is a part sitting IN something. Every real slide switch has that flange, because the actuator has to hold itself in the panel',
      'THE MATERIAL IS THE KIT\'S OWN AND THERE IS NO METAL IN IT. This kit has no aluminium anywhere and the switch does not get to be the exception — a bright machined bar dropped into a language of anodised plates and moulded keys is a part borrowed from another machine. It is a moulded grip: a dark face, a hard bright chamfer top and left where the lamp is, falling to shade bottom and right, and a six-stop face because an extrusion has a chamfer, a face, a turn and an under-edge and a two-stop gradient can only say "lighter at the top". The contrast lives in the EDGE and not in the face, which is also how a real one works',
      'THE FLOOR IS DARK IN ITS OWN CORNERS, both states, because a floor meets its walls in shadow — without that it was a coloured rectangle with a border-radius, the flattest thing in the part. And when it is lit it BOUNCES onto the actuator FROM THE LEFT, because that is where the light is: throwing the switch on puts the actuator at the right end and uncovers the floor to its left, so the lit surface is beside the part and not beneath it. Lighting the underside was a second light source nothing on the panel can account for — the exact failure this language spends its whole shadow budget avoiding. It is the only place here where a colour reaches something it is not printed on',
      'THE KNURL IS THREE RIBS, NOT A PINSTRIPE. A 3px repeating gradient over the whole face is about seven one-pixel ridges at this size, which the eye reads as noise rather than as grip and which turns to grey mush on a dark cap. A real grip has a countable number of grooves, cut into the middle of the part and stopping short of the edges where the moulding radius is. And each groove is a CROSS-SECTION rather than a stripe: bright on the left flank, falling through the crown, into a dark floor and back up — five stops per 5.2px, ramped rather than stepped, because stepped stops give flat bands and flat bands are exactly what cheap looked like', 
      'WHAT THE ACTUATOR UNCOVERS IS THE STATE. A slot has a floor, the floor is painted, and throwing the switch across exposes it — the colour was always there, the actuator was on top of it, which is a real switch rather than a drawn one. It burns in `--lyr` when the switch belongs to a layer and in the panel\'s accent when it does not, the same fallback the pipe takes. And the slot is a rounded rectangle, not a capsule: at a capsule radius the actuator stops being a machined part and becomes a dot in a pill, which is the shape every operating system draws for this',
      'THE LAMP IN THE MIDDLE OF A BLANK CAP WAS THE WRONG ANSWER AND IS GONE, having existed for one unreleased version. It says "lit" where this panel already has jewels, pipes and lit arcs saying that, and it turned every switch on the plate into a dot. `swBtn` takes an icon again and only an icon',
      'IT IS NOT A `.row`, WHICH IS THE OTHER DECISION IN IT. A row is a MODULE CARD, and a card is a claim: a thing with contents, a colour, an editor behind it, a chevron to open it. A boolean has none of that. A settings bay engraves its names straight onto the metal and mounts the control on the face — nineteen booleans each on their own raised card is a stack of pallets rather than a panel. The plate is transparent, the metal warms under the pointer, and a cap presses when the LINE is pressed, because the name is what you were aiming at',
      'IT REPAINTS RATHER THAN REBUILDS, so it carries `.set(v)` like every other value control in the kit. `swBtn` bakes its state into the class, the aria AND the title at construction — three facts, one of them stale the moment a class is flipped by hand — so it has to be replaced wholesale and its container has to be in `ENGAGE_ROOTS` for `engage()` to find the replacement. `toggle` keeps the state on the plate and repaints in place. `.set()` moves the switch without firing the handler, because a config load is not a user pressing nineteen switches',
      'SYSTEM 06 IS THE TOGGLE, three cells and two parts: a setting under a bay cap, a layer column with the master over it, a gate at the top of the bay it governs. The layer column deliberately comes out mixed — two caps and one slide — because the glyph is worth having when it is honest and the row without one is not going to be handed a fake to make the column look even. The master keeps the eye, because a slide has no third position and inventing a middle detent would be a mechanical answer to a logical question',
      'THE TABLE SAYS WHEN NOT TO REACH FOR ONE, which is half of what the section is for. Two NAMED states are an interlock, not a switch: "Portrait / Landscape" is not "portrait: on". A chevron shows content and carries no value. A module with an editor behind it is a `.row` and belongs in the dock',
      'DECLINED, AND WRITTEN DOWN AS DECLINED — a gate turns off two knobs and neither of them greys. It is not built because no app dims a gated bay yet, and a rule this page states while the language does not implement it is drift wearing a hat. It is filed instead',
      'THE PRIMITIVES GRID GAINS THE SLIDE SWITCH, and the backlit-key tile beside it stops posing three DIFFERENT glyphs. It had an eye, an eye struck through and a cells icon in its three positions, so it read as three switches rather than one switch in three states — and the struck-through one contradicted the rule written into `swBtn` itself: off is the plain drawing at its normal weight, because the cap being UP already says off and a slashed grey glyph reads as DISABLED',
      'And `toggle` is named on HOME, in the list of what `dist/skew-kit.js` publishes. A factory an adopter cannot find is a factory they will write again',
      'Sections 06 to 10 are 07 to 11, and the four cross-references that pointed at them moved with them — two to the nav bar inside SYSTEM, one from PANELS at the interlock, and the lede\'s own map of the page',
    ],
  },
  {
    version: '1.31.0',
    date: '2026-08-18',
    changes: [
      'THE HATCH ENGINE IS A FILE. `src/skew-hatch.js`, loaded with one tag, defining KIT, HANDS and HATCH and touching nothing else. It had been living inside a documentation page: KIT declared halfway down one specimen rig, HANDS filled in by a second through a `let` declared 700 lines above it, the machine fills private to a third. Three consequences and the third is the one that made this a file. The hatch bay depended on a specimen rig having run first. Nothing could use the generators without loading a 215KB document. And a hatching system is the one part of Skew meant to KEEP GROWING, which is a thing you do to a file you can open',
      'IT IS VERIFIED WITHOUT A BROWSER, which is new here and is the point of the split. `node -e "require(\'./src/skew-hatch.js\')"` drives the whole pipeline with no DOM present: KIT.parallels gives 49 passes on a 90x70 sheet, KIT.carve at tau 0.36 leaves 17 runs, HATCH.stitch chains those into 6 strokes and saves 11 pen lifts. A generator that can only be exercised by looking at a page is a generator nobody can change with confidence',
      'SKEW HAS A FOURTH PAGE, HATCH, and it is not an exploration. The other three are a language and two apps drawn in it; this one is the engine. The contract, the three layers, the two live specimens at 1:1, the full API, and a section on how to add a hand, a machine fill or a subject without breaking the two rules that are not tunable',
      'AND THE ENGINE HAD BEEN DOCUMENTED TWICE. SYSTEM carried a three-row table of KIT / HANDS / machine fills plus the millimetres rule, which was a strict subset of PANELS\' fill section: two places to change and the short one is the one that rots. SYSTEM is ten sections now, PANELS is seven, and the subject has one home',
      'HATCH-API.md opened by pointing at sections 07 and 08 of skew-layers.html, a file and a numbering that stopped existing three versions ago. The page links it and the file is where the rules that are not tunable stay written down',
      '`scanlines` was private to the hatch bay and is now HATCH.scanlines, which is the export HATCH-API.md had listed as Open since it was written',
      'dist ships three files. skew-hatch.js is COPIED rather than built: it is already a self-contained IIFE with no doc-site rules to strip and no public surface to append, so a build step could only introduce a difference between what is tested and what ships',
    ],
  },
  {
    version: '1.30.0',
    date: '2026-08-18',
    changes: [
      '`.seg` IS DELETED. It was written from scratch, documented as a new part and shipped in dist, and it never once rendered upright: four attempts, each fixing the well, then the panel round it, then the key face, then the specificity, and each time two of the three specimens vanished or the last tab hung outside the chassis. The rule it broke is the oldest one in this repo. If a component is not in a real app, it does not belong in Skew, and nothing in any app was ever built from it',
      'SYSTEM 06 IS THE INTERLOCK, AND ALL THREE SPECIMENS ARE LIFTED WHOLE. The bar is Portrait-Typo\'s page switcher, three positions, `.piano`. The column is that app\'s tool palette, `.palette`, which is a separate object because it answers what a DRAG does while everything in the viewport panel answers where the CAMERA is. The wrapped bar is the hatch bay\'s own, `.piano.sm.hb-tabs`, six names and ALL. Not one of them is built for this page: a specimen written for the documentation is a specimen that can be upright here and broken in the app, which is exactly how `.seg` came to exist',
      'AND `ALL` IS A `key` WITH `latch:true`, NOT A `pkey`. It does not select a surface, it selects every surface, so nothing pops it out and it sits outside the interlock: on the end of the bar, spanning both rows, behind a `.chan`, which is the milled channel that separates a position from something that is not one. It had been given a second well of its own, which said it was a second SETTING sitting near the first',
      'THE RIG IS THREE WIDTHS AND NOTHING ELSE. `.piano` and `.palette` size themselves; the wrapped bar is `width:100%` because in the hatch bay it fills a panel, so on a stage it needs a number to fill. That was the whole of what kept collapsing. A bar sized by its keys while its keys are sized by the bar has no answer, and every fix before this supplied the number from a chassis wrapped round the outside, so the specimen only stood up inside a case it is never inside in an app',
      'The 06 table names the real classes now, with the rows that say when NOT to reach for one: a rotary for an ordered scale, a drum past six, a switch for things that are independent. A trap row records what `.seg` cost, because a part that was documented and removed is worth one line',
      'dist drops three doc-only names instead of five, and ships `.piano`, `.palette` and `.hb-tabs` as it already did. Adopters vendoring the CSS should re-extract: `.seg` is gone from it, and nothing was using it',
    ],
  },
  {
    version: '1.29.0',
    date: '2026-08-18',
    changes: [
      'EVERY PAGE IS ORDERED AS A BUILD-UP NOW, because a reference is still read the first time. The rule was that this site is reached into rather than read through, and that argued the entry point first and the material rules behind it — so a reader met §03\'s knobs, §04\'s segment and §06\'s panel before anything had told them what a proud plate or a milled well IS. SYSTEM goes: the calls you make, the four surfaces every part is cut from, the names for them, the parts, the most-used part in all five of its shapes, then the compounds, then the two sections only a plotter opens, then the traps. Each step is made of the one before it',
      'PANELS IS ORDERED THE WAY THE APP IS USED: the dock, one layer opened, the view onto the work, then the hatch bay — the largest thing this language has assembled — then the fill engine behind it, then a behaviour any panel can wear. Launchpad moved from §04 to §07: it is a second app borrowing the language, not a panel of the one being documented, and sitting between the viewport and the hatch bay it read as one',
      'MACHINE IS ORDERED THE WAY A JOB RUNS: put the head somewhere, choose a pen, commit, then watch. Run moves ahead of the overrides, because an override is something you reach for while it is already running. The four read-only sections are together at the end and in the order you look at them — what is true now, how far through, what went wrong, what happened',
      'AND MACHINE §01 AND §02 ARE NAMED. They were Option A and Option B, which says nothing to anyone who has not already read both: they are a jog PAD and a jog STICK, discrete against continuous. The framing they carried is worth more said once in the lede, where it belongs — a console ships one of them, so the two sections are one decision rather than two parts',
      'SYSTEM HAD NO LEDE AT ALL. It is the page a reader lands on and it opened with a kicker and a word. Both it and PANELS now say in one paragraph what order they are in and why, which is the thing a numbered list of eleven sections cannot say about itself',
      'FIX — six cross-references were already pointing at the wrong section before anything moved. navBar was filed under Section 06, which was the settings panel. PANELS pointed three times at a section 08 that had been the fill engine two layouts ago and is now the bill, and once at a Section 07 that had been the hatch bay. A section number in prose is a link with no href, and this is what that costs: the ones that name the section as well as the number are the ones that cannot rot silently',
      'And the numbered banners inside the page scripts were repointed with them. They had drifted to 01, 01, 04, 06, 07, 02, 08 and 02 across eight blocks — no two agreeing with the headings they build',
    ],
  },
  {
    version: '1.28.1',
    date: '2026-08-18',
    changes: [
      'SYSTEM §04 IS THREE MODULES AGAIN. One chassis with three bays inside it documented the CHASSIS: you read a panel with things in it, not three shapes of one part, which is the entire subject of the section. Three cells on the stage — a column, a strip, a wrapped bar — each captioned above and tagged with its class below',
      'AND THE DEFINITE WIDTH MOVED TO THE CELL, which is the thing that had been breaking over and over. A well is sized by its keys while its keys are sized by the well, and loose on a stage that circle has no answer. Every previous fix supplied the number from a panel wrapped round the outside — so the specimen was only upright while it sat inside a case it is never inside in an app. Three widths, one per shape, on the cell',
      'THE CHASSIS WAS ALSO PAINTING THE SPECIMENS. `.box::before` is a grain-and-pinstripe overlay on `mix-blend-mode:overlay`, and the panel body had been built without `box-body` — the class that carries `z-index:2` — so everything in it was being painted through the overlay. Adding `box-body` lifted the controls clear and left the overlay blending against nothing, which turned the whole case white. A stage has no overlay and there is nothing here to get wrong',
      'FIX — `.segpanel` and `.segpanel-body` had never once applied, for the whole life of that rig. One class each, the same weight as `.box`, `.box-body` and `.set-body`, all declared 1400 lines further down the file: at equal specificity the later rule wins, so the panel was always `.box`\'s 318px and never the 352px written for it. Both classes are gone with the panel',
      'THE SEGMENT KEY IS THE PIANO KEY, ONE SIZE DOWN, and it had been given the nav bar\'s instead. `.piano.sm` had these numbers already — 26px cap, 9px face at .06em, a shallower skirt, no travel — and it is what every strip that ships in an app is built from. `.seg` inherited bare `pkey`: 36px caps at 11.5px/.12em, sized for four words in a nav bar. Five tabs at those numbers measure about 370px in a 294px well, which is why the strip ran out through the side and the last tab sat on the page. The legends are engraved in caps, which is what the shipping strips do and what the rig\'s mixed-case labels were quietly not doing',
      '`All` LOST ITS SECOND WELL. It is not a sixth position — it selects every surface rather than one — but a well of its own said it was a second SETTING sitting near the first. It goes on the end of the bar, outside the interlock, spanning both rows, exactly as the hatch bay builds it. And the rows are explicit with the span written as a span: `1 / -1` counts back from the end of the EXPLICIT grid, so with implicit rows it collapses and `All` sits in the top row with the names flowing round it',
      '`1fr` tracks are right now and were not before. A fractional track needs a definite width to divide, and in a shrink-to-fit box there was none — which is what a hand-measured 86px column had been covering for',
      'FIX — `.segrig` was a class rule and the markup carried `id="segrig"`, so the centring never ran; the stage had been centring it by accident',
      'The cell modifiers are prefixed — `.segcell-col`, not `.segcell.col` — because tools/build-dist.mjs drops a rule only when every class in the selector is doc-only, and `col`, `strip` and `wrap` are names an app is entitled to use',
    ],
  },
  {
    version: '1.28.0',
    date: '2026-08-17',
    changes: [
      'THE NAV BAR WAS ON TWO PAGES. SYSTEM specified it and PANELS showed it again wired to the app, which is one component in two places to change. It is SYSTEM\'s, and PANELS lost the section. PANELS §01 also called itself "The panel" under a note about the light source at 158 degrees, and was neither: it is the layer dock, and it says so now',
      'SYSTEM IS ORDERED BY WHAT IS REACHED FOR, not by what is foundational. A reference page is read by reaching into it, not by reading through it, so the entry point goes first: the factories, then what they render as, then the layouts of the most-used control, then the compounds every app needs — segment, nav bar, settings. The material rules that justify all of it sit behind them at 07 and 08, and the two sections only a plotter app opens, the room and the hatch, sit at 09 and 10. Traps last, because you arrive there from a bug rather than from the top',
      'Every cross-reference between sections was repointed with the numbers, on both pages. A section number in prose is a link with no href and nothing tells you when it rots',
      'THREE SECTIONS WERE SHOWING THE SAME TWO OBJECTS. The light section posed a plate and a well under the lamp, the recess section posed the same plate and well against each other, and the primitives grid carried a tile for each — so you met the chassis three times before reaching a control, and the third time told you nothing the first had not. They are one section now: SYSTEM §01, Surfaces, one stage with four specimens on it and the two rules read off them. SYSTEM is eleven sections, down from twelve',
      'The four that moved are the four that are surfaces — chassis, well, stage, engraving — and the primitives grid keeps the parts that are not: Light, Setting a value, Pressing. Fourteen tiles instead of eighteen, and none of them a repeat of §01',
      'THE STAGE IS A DOCUMENTED SURFACE NOW, and it should have been from the start: this site had been posing proud against recessed ON a third surface it never named. A chassis is a raised body, a well is a cut into one, and a stage is neither — it is a FIELD. Same case metal at low light, a hairline instead of a bevel, one soft radial off the lamp so it is not flat. Nothing bolts to it and nothing cuts into it; things are set down on it',
      'It is in the Material group as a specimen and in §03\'s table as a row, where it can say the thing that makes it obvious: the panel that row is printed on is the example. Every specimen on this site sits on one, and so does the drawing area of an app',
      'SECTION 07 IS ONE PANEL, AND THE REFERENCE IS PORTRAIT-TYPO\'S DROP PANEL: a single chassis, engraved headings down it, the controls each heading owns underneath. That is what a segment is actually surrounded by, so that is what it is shown in — three boxes, then one box with bays, then three cells loose on a stage were all arrangements invented for a documentation page rather than taken from a panel',
      'AND THE PANEL IS WHAT FIXES IT. Posed loose on a stage a well has no width of its own: it is a flex item asking its keys how wide it is while the keys ask it back, so the column and the tab strip collapsed to nothing and only the grid, which had definite tracks, survived. Every attempt to fix that by tuning the wells was tuning the wrong half. A panel has a width; everything inside it can be measured against something',
      'FIX — .segpair had been added to the build\'s exclude list by a duplicated line, so the one rule that makes All run full height beside its strip was being stripped out of dist. The list had grown two copies of itself; it is one now',
      'That rig had already been three boxes, then one box with three bays, then three cells loose on a stage. Each fix tuned the wells and none of them touched the thing that had no width, which is why it kept coming back',
      'And every specimen-bearing section now sits on the same object. §02\'s sheet was floating on the page; it is on a stage too, which the loupe needed anyway — a glass overhanging the paper has to overhang ONTO something',
      'SYSTEM §08 WAS ONE PANEL WITH THREE BAYS, not three panels. Three boxes side by side made three chassis for one part and gave each a third of the width, which the six-name strip does not fit into. A panel with three captioned bays is also what an app actually builds: nobody gives a tool palette its own case',
      'FIX — the wrapped strip was laying out on `repeat(3, minmax(0,1fr))` inside a shrink-to-fit box, so the fractional tracks had nothing to resolve against. The columns collapsed to nothing and every legend spilled out across its neighbour. Definite columns now, at the width of the longest name in the set',
      'THE LOUPE\'S MAGNIFICATION IS DERIVED FROM THE PITCH, not fixed. A constant 6x is right at one grid setting and wrong at every other: coarsen the grid and the glass holds a quarter of a square, tighten it and it holds nine, so the one thing a loupe exists to show keeps sliding out of it. It solves for 1.6 major cells across the glass at any pitch — enough to see a corner with the neighbouring rules on two sides, where at exactly one you are inside a cell with no major in view. The bezel mark reads the computed figure',
      'FIX — merging §08 into one panel cut the `wrapped` builder out with the cell function it sat next to, and the section threw a ReferenceError that killed everything after it. Restored, and the page script now audits clean for free variables',
      'The loupe moved to the top right of the sheet, which is the corner a hand reaches over rather than the one it covers',
      'FIX — version.js had stopped parsing. An edit left a doubled `],` in the changelog, so every page\'s nav window sat at its v— placeholder: the pages read the version with a dynamic import and the import was throwing, silently, behind a .catch that exists so a bad import is a dim window rather than a dead script',
      'AND THE BUILD NOW IMPORTS IT RATHER THAN GREPPING IT, which is why this was invisible for several commits. tools/build-dist.mjs matched the version with a regex, so it read a perfectly good number out of a file that no longer ran and reported "clean at v1.28.0" while every page disagreed. A build check that cannot fail the way the page fails is not a check. It exits 1 now, with the parse error',
      'package.json main pointed at index.html, which moved to docs/ in 1.27.0. Not "type":"module" though — that was tried and it kills server.js, which is CommonJS on purpose, and a MODULE_TYPELESS warning is cheaper than a dead launcher',
      'AND THEN PANELS LOST THE TABS SECTION ALTOGETHER. Renaming it from "the page switcher" was right and keeping it was not: the strip is specified in SYSTEM §08 as .seg.inline, and a second copy on an app page is a second place to change it and a second place to be wrong. PANELS is nine sections now, renumbered, and the hatch bay\'s own comment points at §08 instead of at a section that no longer exists',
      'It was called the page switcher and it never was one — what it switches is which face of THIS panel you are looking at. The panel stays, its contents change, nothing navigates and nothing loads. A page switcher moves you; these move the panel. That distinction survived into §08\'s table, which is where it belongs',
      'PANELS §03 WAS THE TABS, not "the page switcher". It never was one: what it switches is which face of THIS panel you are looking at — the panel stays, its contents change, nothing navigates and nothing loads. A page switcher moves you; these move the panel. And it shows Portrait-Typo\'s real five, Letters Place Size Drop World, because three invented names are a picture of a tab strip rather than the strip',
      'The object is SYSTEM §08\'s segment lying down, and it says so — same well, same pkey interlock, documented there as the language and shown here as what the panel actually wears',
      'A THIRD SEGMENT VARIATION IN SYSTEM §08 — and in §08, not in PANELS, which is where it was built first and where it did not belong. PANELS is one app; a variation of a part is the language. The wrapped strip: past about five names a strip stops fitting a line and the answer is a grid, not a smaller face. It stays ONE well, because the well is what says the six are one setting, and split in two it becomes two settings that happen to sit near each other',
      'AND `All` IS NOT A SIXTH POSITION, so it is not in that well. It does not select a surface, it selects every surface — a different KIND of answer — and inside the interlock it would be mutually exclusive with the thing it exists to include. Its own well, full height, hard against the strip: near enough to be the same subject, separate enough not to be one of its positions. Portrait-Typo\'s hatch bay is where the shape came from',
      'LAYERS IS PANELS. The document is about how a panel is built — the chassis, the dock that seats in it, the page switcher, the editor, the viewport, the bay — and it was named after the one app feature it happened to open with. A reader arriving at LAYERS is looking for a layer part; what is in there is the anatomy of a panel with a layer app posed in it. Renamed with git mv, file and key and title, and the card lists the sections it actually has',
      'TITLES ARE TITLES. Every heading on all four pages carried a second clause after a dot — "The light · one lamp, and nothing argues with it" — which is an aside written for nobody, in a voice that is not mine, on the one line of a section that has to be scannable. The name is the title. Thirty-eight of them, gone',
      'FIX — the grid bay was writing --grid-n while the CSS had moved to --grid-pitch, so the knobs drove nothing and the sheet sat at its default. Changing a contract on one side of a file boundary and not the other, in the same hour I wrote both',
      'FIX — and the staircase across the sheet was the second half of it. A repeating gradient at a fractional repeat does not land on the device grid, so the renderer rounds every pass on its own and the error walks. The panel does the arithmetic now: a minor pitch in whole pixels, and the major built as minor x sublines so the two cannot drift apart. CSS is handed integers',
      'FIX — cells were not square. Dividing each axis into the same COUNT gave 15 x 21 cells on a 300 x 424 sheet: a grid describing the sheet\'s proportions rather than a unit of measure, and you cannot count a distance on one of those. One pitch, both axes',
      'FIX — both weight knobs were dead across three quarters of their travel. Weight was clamped to a 1px floor, and the range is 0.1 to 1.2mm, so everything under 1 came out identical. Under one pixel it is ALPHA and over it is width: a line thinner than the device can draw is not a thinner line, it is a fainter one, which is what a finer pen does to paper anyway',
      'The bay knobs were layout:bare, which shows the pointer and no number — on a control whose whole subject is tenths of a millimetre that is a control you cannot read. All four are compact, and a Pitch readout says what the sheet actually got, since Lines is what you ask for and that is what it can be ruled to',
      'THE LOUPE IS A LOUPE — round, lying on the paper, overhanging the sheet\'s corner and throwing its own shadow onto the desk. A rectangular inset off to one side was a second picture you had to relate to the first; a glass ON the sheet is the same object as everything else here, a thing on a table at 1:1 that you would pick up. Round is also the one silhouette on this page that cannot be mistaken for a panel, so nothing about it reads as another control to operate',
      'Three parts, same as every other object: a machined bezel with the lit edge on top, glass cut INTO it rather than laid on it, and a specular running off the same 158 degree lamp as the room. The magnification is engraved on the bezel, because it is a property of the instrument and not a value the panel sets',
      'A 6x loupe beside the bay. Every number in that panel is a fraction of a millimetre on a 1:1 sheet, so the thing being adjusted is smaller than the control adjusting it, and a weight rounding up to one device pixel is invisible at 1:1 and obvious at six',
      'FIX — stripping the subtitles took a live element with them. SYSTEM section 05\'s heading carried <span id="mat-count"> inside the clause after the dot, and the regex that cut the clause cut the span. materials() then threw on a null, which killed EVERY IIFE after it in that script: the drawer, the settings panel, the knob layouts, the grid bay and the segment all stopped building. One throw at the top of a shared script is the whole page below it',
      'The count is gone rather than restored. It read "eighteen parts, at their real size", which was the kind of clause the strip existed to remove, and a number the page counts for itself is a number nobody was reading',
      'SECTION 10 IS TWO COLUMNS, the spec on the left and the control it describes on the right. Stacked, the panel was a wall of definitions with the thing they are about below the fold — you read the spec for a button you had not seen yet',
      'FIX — SYSTEM section 10 carried the whole "one control, two positions" stage twice, duplicate id="setopen" and all, so one of the two was a dead button. Removed, and the surviving pair moved to the right of the spec panel instead of under it',
    ],
  },
  {
    version: '1.27.0',
    date: '2026-08-17',
    changes: [
      'src/ dist/ docs/ tools/, which is the layout every library of this shape uses and the half of the job 1.26.0 left undone. dist/ arrived without the rest, so the root still held the source, the site and the build output in one list and the folder that was supposed to answer "which files are mine" was sitting next to nine that were not',
      'src/ is skew.css, skew-kit.js and skew-machine.css. docs/ is the four pages plus skew-home.css and skew-nav.js, which belong to the site and never to an app. tools/ is the builder. dist/ is unchanged and is still the only folder an app copies from — that is what having it is for, and it is why this move does not break the thing 1.26.0 broke',
      'NOT js/ AND css/ AT THE ROOT. Splitting a library by file type is the convention that src/ replaced; what sits inside src/ is split by component, not by extension, once there is more than one file to split. skew.css is one 3400-line file with 23 section banners, so that day has not come and the banners are where the seams already are',
      'THE DOCS READ src/ RATHER THAN dist/, deliberately. A documentation site built from the build output can document a version of the language nobody has shipped yet, or worse, keep looking right after the builder breaks. Reading the source means the pages fail the moment src/ does',
      'THE SERVER RESOLVES AGAINST docs/ FIRST AND THE ROOT SECOND, instead of redirecting / to /docs/. A 302 puts a build directory in the address bar of the thing a reader was sent to look at. So / and /skew-system.html come out of docs/, and /src/skew.css, /dist/skew.css and /HATCH-API.md fall through. The traversal guard still tests one root, because docs/ is inside it',
      'The papers moved out from under the pages, so their links carry a ../ that the reader does not want to see. The chip under a paper is the file NAME now and the href is the path — they stopped being the same string and the badge was the half that should not have changed',
      'A README, which this repo did not have. The layout, how to adopt it, how to build it, and the three rules that are actually rules',
      'SYSTEM §02 SHIPS PARTS NOW INSTEAD OF DESCRIBING SOMEBODY ELSE\'S. The room and the sheet went in as prose and a table read out of Portrait-Typo, and skew.css had no selector for any of it: thirty mentions of sheet, paper and desk in the file and every one of them in a comment. A section that documents an app rather than the language is a section that reads well and gives you nothing',
      '.sheet is the paper: white, two shadows, and nothing reaching inside it — no grain, no tint, no filter, no blend. The shadow is ten times a panel\'s throw and that is not decoration: same wide-plus-contact recipe every raised thing here uses, and at panel numbers a white sheet on a near-black desk reads as a sticker. .sheet.window drops it for a 3D view or a depth map, which is a window onto something rather than paper on a desk',
      '.ruler-tick and .ruler-label, plus the minimum strip to hang them in. This is the one place the engraving rule inverts: everywhere else a cut is darker than its surface with a lit far wall, and on a near-black desk a near-black tick is not a tick',
      'THE GRAIN\'S Z IS A TOKEN. --grain-z defaults to 99, which is right on a page with no floating panels and wrong in every app, where the grain belongs under the panels and over the canvas — Typo runs 90 against panels at 96 and up. It was a hard-coded 99 that an adopter had no way to reach, and the symptom is a haze over your own panels that you go looking for in the panel\'s CSS',
      'The specimen is live, and its size is inline on the element rather than a .deskdemo .sheet rule, which found the exclude list\'s one sharp edge: a selector naming a doc class AND a part is kept, because dropping .mat .knob would take a part\'s geometry with it. Right in general, and here it would have shipped a dead rule sizing a sheet inside a container no app has',
      'THE SEGMENT IS A PART, SYSTEM §08, out of the feature request and into the language. pkey in a well: the keys read as one setting rather than as three buttons. Two orientations, live, built from pkey so the specimen cannot claim an interlock the factory does not have. One line of prose and a four-row table saying when to reach for it against a rotary, a drum or a switch',
      'THE PROSE IS GONE FROM ALL FOUR PAGES. Thirty-eight description paragraphs removed, plus the eighteen the primitives grid carried under its tiles and .mat-d with them. What is left is a title, the thing itself, and a table where there is genuinely a rule to look up. The specimens are live; a paragraph explaining a control you can press is a paragraph nobody reads',
      'The segment was a tile first and that is what made it obvious: at 232px a paragraph is forty lines and the control ends up at the top of a column of grey. It is a section now, which is what a compound part gets',
      'THE SEGMENT SPECIMEN GETS ITS PANEL AND ITS STAGE. A well needs a surface to be below, and on the page\'s own near-black desk it had neither — the chassis lost its cast shadow into the ground and the whole thing read as a floating outline. So each one is a real box with an engraved cap, posed on the same lit plate the tiles use. The box overrides .box\'s 318px, because a tool palette at the width of a settings pane is not a tool palette',
      'THE SHEET HAS ITS GRID, AND THE GRID HAS ITS BAY. Paper you are about to plot on is not blank paper, and the specimen was: a white rectangle with rulers beside it. The grid is the only thing on the sheet that is NOT the work, which is the whole difficulty — readable enough to align against, faint enough that a rule is never mistaken for a stroke',
      'TWO WEIGHTS, AND THE SECOND IS THE POINT. A single pitch is either too coarse to place anything against or dense enough to grey the page. Majors carry the count and sublines subdivide them, and the sublines are thinner AND fainter: below about 0.3mm a line stops getting lighter as it gets thinner, because the renderer floors it at one device pixel and draws it at full strength, so weight alone cannot make a hierarchy and the alpha has to do the rest',
      'Every number is a custom property, because every one of them is a setting: the panel writes and the CSS draws. Lines, sublines, both weights, opacity and colour, live, beside the sheet — a grid you cannot change is a picture of a grid, and you can watch the sheet go from useful to unreadable in about two turns of the Lines knob. Counts on knobs with detents at the pitches a sheet is actually ruled to, opacity on a fader because it is a length from nothing to all of it, and the colour opens the picker',
      'The sheet in §02 goes from 132px to 300px. A specimen about how paper sits on a desk is about the shadow, and the shadow was smaller than the caption under it',
      'THE PRIMITIVES ARE SORTED. Eighteen tiles in one grid is a dump, so they are five groups by the QUESTION the part answers, which is the question you arrive with: nobody goes looking for a fader, they go looking for how to let someone set an amount. Surfaces, Light, An amount, One of many, A press',
      'EACH GROUP IS A STAGE, the same lit panel §07 poses its knobs on. Tiles straight on the page have nothing holding them together and five groups read as one long list with headings floating beside it; on a stage the group IS an object, and the caption is the stage\'s own note rather than a heading that could belong to either side of the gap above it',
      'AND A TILE IS NOT A PANEL. It was a card with its own border, its own body and its own lit stage, so every specimen sat on a little plate inside a big one — two chassis for one part, and the part came third. The tile is the specimen and its name now; the group\'s stage is the only surface, which is what a stage is for',
      'FIX, and it shipped broken for one build: the container kept the .mats class while the groups nested their own .mats inside it, so every heading and every group became a grid item of the outer grid. Five columns of headings interleaved with five grids. A container that is also a layout is a container that fights whatever it holds',
      'AND IT IS NOT A GRID, which took three tries to admit. A grid gives every cell the same width, so a 60px knob and a 300px fader get the same track and the knob sits in a lake of its own column. Eighteen objects of eighteen sizes want the layout that asks each how wide it is: a wrapping row, packed left, at natural width. Column spans, dense flow and a per-group column count were all ways of telling a grid about sizes it could have measured itself',
      'A tile named in the array and not named in a group would simply vanish, and the count in the heading would go on agreeing with itself. It warns instead',
      'Three groups rather than five: Material, Setting a value, Pressing — six, seven and five, which is a stage worth being a stage rather than three tiles and a lot of air. The drum and the colour picker moved out of "a choice" and in with the faders, because a list you pick a value from is setting a value; the merge that put them beside a micro key was sorting by shape rather than by what the part is for',
      'Order inside a group is narrow first and wide last, which is a packing rule rather than a taste: the row wraps left to right, so a 300px fader sitting between two 60px parts leaves the tail of its line empty',
      'AND THE TILES GROW TO FILL THE LINE. Packed left at their natural width they left the tail of every row empty, which on a stage reads as the group having run out rather than as the group being laid out. They share the slack now, so the air lands BETWEEN parts instead of all of it after the last one, and a wide part asks for its real length as its flex basis so a knob and a fader bank do not split the slack equally',
      'EVERY SPEC TABLE FOLDS, SHUT BY DEFAULT. These pages are specimens first: the argument for a part is the part, live, at its real size, and a four-row table of prose under it is what you read once and scroll past forever after. Folded, a section is its heading and its parts, and the reference is one click away for the day you need it',
      'A <details> does the mechanism rather than a hand-rolled toggle — keyboard, screen reader, Escape and find-in-page opening it to show a hit are all in the platform, and every version of this written by hand gets at least one of them wrong. What is added is the metal: the summary is a milled strip rather than a key, because it is a lid and not a control that does anything to the machine, and the chevron turns the same 90 degrees a layer row\'s does. The row count sits on the right as a readout, which answers the only question a shut lid raises',
      'The label is the table\'s own first heading, so a fold cannot drift from what is under it, and a table with no thead is a trap block and says so',
      'FIX — .mat-t and .mat-stage had been deleted outright by an earlier range edit that took out a stale comment and the two rules under it. The names fell back to browser <p> defaults, so every part on the page was labelled in 15px body type, sentence case, louder than the specimen it named. Restored as engraved captions',
      'FIX — the tiles used align-items:flex-start, so each was only as tall as itself and the names sat wherever their specimen happened to end. In a wrapping row every LINE sizes to its tallest item, so stretch plus margin-top:auto on the body drops every name in a line onto one baseline',
      'The engraving specimen said LAYERS, which is the name of another document and means nothing on a tile about cut metal. It says SKEW',
      'A TAB STRIP IS A SEGMENT LYING DOWN, and §08 now says so where somebody looking for the word "tabs" will find it. It was already true — Portrait-Typo styles .led-tabs and .lp-tabs out of the same well group as .vp-seg — and true-but-unfindable is the same as missing. Three rows: .seg for a palette, .seg.inline for a panel\'s own sub-views, and navBar view mode for views of the whole page rather than of one panel',
      'The two specimens show the two real uses instead of one rotated: a tool palette with icons and a cap, and a five-tab strip with neither. A tab strip is the top of the panel it belongs to, so titling it would title the panel twice. And the tab keys are tighter than pkey\'s nav-bar defaults — 22px of side padding and a 78px floor put five tabs wider than the panel they sit in',
      'HOME §03 — IN USE. Screenshots of a real app, which is the only claim on that page a table cannot make: every section above it is the language describing itself, which is exactly as convincing as any design system\'s own documentation. Two full views and four panel crops, all Portrait-Typo — two apps at one view each would say the parts are portable, one app at six says the parts hold a whole tool together, which is the harder claim and the one in doubt',
      'The crops are shown at their OWN size and the screens full width, because they are asked different questions. A full screen asks whether the parts cohere; a crop asks whether this many controls in this little space is still readable, and blown to page width every part is at twice the scale it is ever used at, which answers nothing',
      'One caption says the panel is half converted, on purpose. LETTERS has the font on a drum and the set, the characters and the extrusion still on a native select, a text field and a range input. A language is adopted control by control and that is what it looks like; a gallery that only shows the finished panels is a brochure',
      'A missing shot is a DEAD SCREEN rather than a broken image icon — powered, showing NO SIGNAL and the file it wants. The panel around it stays correct, so the page never looks broken while a file is on its way in',
    ],
  },
  {
    version: '1.26.0',
    date: '2026-08-17',
    changes: [
      'THE FILES ARE CALLED SKEW NOW. physical-ui.css is skew.css, physical-ui-kit.js is skew-kit.js, and the nav, the home stylesheet, the machine stylesheet and the three documents follow. The name physical-ui was the name of a question this repo asked once and has since answered; the language is called Skew everywhere else, including inside the two apps that vendor it. Renamed with git mv, so the history follows',
      'BREAKING FOR THE ADOPTERS, and it is one line each. Portrait-Typo reads ../targz-design-system/physical-ui.css in tools/kit-css.mjs; Portrait-ribbons reads that plus physical-ui-kit.js in tools/vendor-kit.mjs. Both paths have to become the skew names before either extractor runs again',
      'SKEW.CSS IS WRAPPED IN @layer skew, which is the fix for a collision this file has been walking into since it was written. It claims .card, .row, .box, .key, .head, .page and .wrap at the top level, so an app with any of those got them repainted. An unlayered rule beats a layered one whatever the specificity says, so an app now keeps its own without renaming anything and without an !important',
      'It also disarms the trap the file\'s own notes describe three times: [data-skin=light] .sw out-ranking .sw.on, a material rule silently un-pressing a key. Layers order by declaration, not by specificity, so state rules written on top can no longer lose to material rules underneath. No page here needed a change: none of the four has an inline style block',
      'RANGEFADER GETS .set(x, y), the last of the five value controls to have it. knob, fader, rotary and drum have carried the handle for versions; the span did not, so a loaded config could push a value into every control on a panel except that one. Two arguments because a span is two numbers, and the far cap is clamped by MIN_SPAN the same way a drag clamps it, so a stored pair that crossed cannot put the control in a state a hand could not reach. Silent, like the others: a host pushing a config is not a user turning a knob',
      'Found by diffing Portrait-Typo\'s vendored copy against the source rather than by reading the version stamp. Typo had written the method itself and the design system never took it back, which is the one place an adoption was ahead of the language',
      'THE HOME SAYS HOW TO ADOPT IT. Section 02: which two files to copy and which three to leave, that you vendor rather than link because Launchpad roots every project at its own folder, the two extractors in Portrait-ribbons/tools that do the copying and report drift first, and the rule that a vendored file is never hand-edited',
      'It also names skew-panel.js, which is the answer to the thing the kit does not do: past about twenty controls, hand-composing a panel stops paying, and a spec-driven builder is what keeps the live binding and the scroll that Tweakpane gives away free',
      'dist/ EXISTS, AND IT IS THE ANSWER TO A QUESTION YOU HAD TO BE TOLD. The root holds eleven files and two of them are for an adopting app; being told which two is not a system. dist/skew.css and dist/skew-kit.js are what you copy, everything outside is what you read. Generated by tools/build-dist.mjs and never hand-edited, because a third copy of a stylesheet is a third thing to keep in sync unless a program owns it',
      'THE CSS BUILD DROPS THIS SITE AND NOTHING ELSE — 35 rules. It does not try to work out which parts your app uses: that judgement needs your code and already lives in your repo as tools/kit-css.mjs. So dist/skew.css is barely smaller than the source and that is correct; the win is that copying it cannot drag .wrap, .spec and .mat into your app',
      'An EXCLUDE list rather than an include list, and the direction is the point. An include list is built by scanning the kit for the class names it writes, and that scan misses the ones only the CSS knows — .knob-ring, .kr-track and .rot-leads are inner parts no line of JS names. Typo\'s extractor documents the failure: the controls still build, still work, and render as flat black discs, because a missing rule does not throw. Over-including costs bytes; under-including costs a control, silently',
      'THE JS BUILD WRAPS THE KIT IN AN IIFE on window.SkewKit. The source is a plain script whose factories are top-level consts, written for a page that loads nothing else; an app running p5 in global mode has its own global key, and two of those collide without throwing. Both adopting apps wrote this wrapper themselves, separately, which is the signal it belongs here. The body stays byte-identical, which is what lets the drift check be exact',
      'SYSTEM GETS A HATCH SECTION, §09, and Traps moves to §10. It is the one part of the language that makes marks instead of light: a plotter cannot shade, so every grey is a decision about spacing, angle and passes, and that decision is the same in every app that ends at a plotter. The section carries the three layers, the one contract — a region is a function tone(x, y) — the three parameters a caller owns, and the millimetre rule as a trap, because getting it wrong still runs',
      'It also says how to grow it: a style added quietly is a style nobody else can reach and a second implementation of something KIT already has, so a new fill is a written request. The three already open are named there — the machine fills are not exported, chaining never crosses passes, and a shaded region cannot use a wall walk until someone extracts its isoline',
      'The home page section 02 now points at dist/ rather than at the root files, and names skew-panel.js as the answer to the thing the kit does not do: past about twenty controls, hand-composing a panel stops paying',
    ],
  },
  {
    version: '1.25.0',
    date: '2026-08-17',
    changes: [
      'THE KNOB HAS FIVE LAYOUTS AND THEY ARE NOW WRITTEN DOWN. It had two, stack and row, documented nowhere, so the only way to find out row existed was to read the factory. SYSTEM §06 is all five side by side and live, and they fall out of two questions rather than a list: which way does it stack, and does it sit on its own plate. stack is vertical on bare metal, TILE is vertical on a proud plate, row is horizontal on a plate, compact is horizontal on bare metal, and bare ducks both',
      'A TILE IS A MODULE RATHER THAN A CONTROL, which is what the plate buys: it is the edge that says where one channel stops and the next begins, so it is what a GRID of knobs wants where a column of them wants the row strip. Same plate recipe as the strip, because they are one decision in two orientations. And 270°, a lit arc and a pointer in every layout, with size, detents, bipolar, fmt, arc and .set() working in all five. Picking a layout is a question about the surroundings, never about the control',
      'FIX — the row strip had no floor under its width. It is width:100%, so a parent narrower than the strip needs squeezes the row until the label and the value climb onto the dial, and what that looks like is a broken knob rather than a container that is too small. Its intrinsic width is 178px and it now refuses to go under it; the §06 demo cell had been giving it 150',
      'And it was crushing the dial as well. The value arc sits at r=43 of 50 with a 3px glow in rendered pixels, so on a 40px knob the light spills about 1.6px outside the element; at 4px of padding against a 9px corner radius the plate edge cut diagonally through that spill. 9px of air and a 13px radius',
      'The place bay opts out of the floor and takes the padding: it runs a narrower value window and shares a 318px box with a rotary, so 178 would push it out of its own panel. It had also overridden the padding straight back down to 4, so it had the same crush',
      'COMPACT AND BARE EXIST BECAUSE CALLERS WERE FAKING THEM. The pen bank built a stack and then deleted .klab and .kval by hand, which is a caller that breaks the day the markup changes and which silently kept the stack\'s 46px and its column gap. It asks for layout:bare now',
      'Both copies of the kit got the change, which is the first time the debt written into physical-ui-machine.html and physical-ui-kit.js has actually been paid rather than declared',
      'THE SETTINGS DRAWER MOVED TO SYSTEM, whole: the aside, the tab, both panes and 383 lines of builder. A settings panel is not a design problem, it is a sorting problem — quantity to a knob, short list to a switch, long list to a drum, two exclusive things to paired keys, a computed value to a readout — and that sorting is the language, not the app. It is §07 there, live, with the spec and the tab mark it already had',
      'The extract needed nothing from the page it left: every call in it resolves against physical-ui-kit.js, and the three suspicious identifiers turned out to be words in comments. Layers is ten sections now and the pointer it used to carry into SYSTEM §07 is gone with the section that carried it',
      'THE NAV GOES WITH THE PAGE when the drawer opens. It is a sibling of .wrap and not a child, so the rule that only moved .wrap left the bar sitting still while everything under it slid, which reads as the bar belonging to the drawer',
      'THE PORT SECTION IS GONE FROM THE MACHINE, and its own opening line said why: "nothing invented, and that is the section". A section on an app page whose entire content is a decision the language already made is a section on the wrong page. Both halves of that decision are already on SYSTEM: a long list is a drum, and a rotary has a hard ceiling of six because its fan is a fixed 80°',
      'THE CONTROL STAYED, next to the console it is the way into. You open a port and then the tape talks, so the drum, CONNECT and the baud window sit on the same stage as the roll rather than in a section of their own. The machine is ten sections now',
    ],
  },
  {
    version: '1.24.0',
    date: '2026-08-17',
    changes: [
      'THE PEN HEIGHTS HAVE A DEFAULT AND A WAY BACK TO IT. TargzPenPlotterCtrl reads them out of localStorage under penUpZ / penDownZ / penPumpZ and falls back to 5 / 0 / −2 when nothing is stored, so the numbers already existed; what did not exist is any route back once you have dragged past one. PEN_DEFAULT is frozen, because a default that can be reassigned at runtime is not a default',
      'A notch at the default on each knob, the same answer as the overrides\' notch at 100: the finger finds it on the way through without aiming and it clacks when it drops in. It was briefly a notch plus a key printed with the number, which was a second control saying what the lit window already says',
      'THE KNOB MOVED LEFT OF THE KEY, because you set a height before you send the pen to it, with 30px between them. They are two different jobs, one sets the height and one goes to it, and at a 9px gap they read as a single compound widget. The chassis came down from 392 to 292: once the key stopped being full width it was carrying 130px of empty metal',
      'THE NAV BAR HAS TWO MODES, and one question picks between them: is the thing behind a key a DOCUMENT or a VIEW of the page you are on? doc gives anchors, aria-current, no tablist and four tab stops. view gives buttons, a real tablist, roving tabindex, arrow keys, and nothing navigates',
      'That is not a contradiction of the "not a tablist" rule, it is the other half of it, and the two common bugs are one bug in two directions: a router faking links with click handlers, or a single-page switcher reloading the document to change a panel',
      'THE SPECIMEN ON SYSTEM §06 IS THE VIEW VARIANT, so a spec you are reading cannot navigate you off the page it describes. It is not a defanged copy either: it is the bar\'s second real mode, doing what it does in a single-page app',
      'THE ANNUNCIATOR NO LONGER FLASHES. A lamp is lit or it is not, and that is the whole state; the flash was carrying a second fact on top of the one the lamp already carries, and a cell changing twice a second is a cell you cannot read while it does it. UNLOCK still acknowledges and still clears what can be cleared, it just has no blink left to stop',
    ],
  },
  {
    version: '1.23.0',
    date: '2026-08-17',
    changes: [
      'FOUR PAGES AND ONE RULE FOR WHICH IS WHICH. HOME is a door and specifies nothing. SYSTEM holds anything that belongs to no app: if a part shows up in both explorations it lives there. LAYERS and MACHINE hold one real app each, and a control stays on its page when it only makes sense next to that app\'s anatomy. Every page that had to answer "what are the parts" now links to the page that does',
      'physical-ui-system.html, and it is DARK like the rest of Skew: the light at 158° and everything that agrees with it, recess against proud as four hairlines, the token table, every primitive at its real size, the factory API, the nav bar spec and six traps',
      'THE PRIMITIVES MOVED OUT OF LAYERS. Materials was §11 of an app document and was the one section there not about that app. It came across whole, and layers is eleven sections now instead of twelve',
      'AND THAT CLOSED THE ONE OPEN BUG. mat-count had no element to write into since the §11 paragraph lost its span, so the last statement of the layers script threw on every load. The section moved to a page whose heading carries the span, so the count writes itself again and the crash has no code left to run in',
      'THE NAV IS ON EVERY PAGE, from physical-ui-nav.js. A page adds a bar with one script tag as the first node in body plus id="main" on its wrapper; the module writes the skip link, the sentinel and the dock. The registry moved in with it, so the bar and the home cards read one array and cannot disagree about what exists',
      'A SKIP LINK, because four destinations in front of the content on every page is four tabs before the first heading. Off screen until focused, then it lands on the metal like anything else',
      'THE NAV CSS AND THE SPEC TABLE MOVED TO physical-ui.css. Both are furniture every document uses now, and a component whose styles live in one page\'s stylesheet is a component that page owns',
      'old/index.html IS DISCONTINUED, not shipping and not a peer. It is one dashed line at the foot of the home, never a key on the bar: a card would put a dead document in the same row as three live ones. It stays reachable because deleting the thing a decision was made against loses the decision',
      'Skew is what is adopted, so "not adopted" is gone from both explorations and so are the claims that index.html is untouched, which stopped being true when index.html became the home',
      'THE DRAWER IS A SYSTEM PART, so its spec left the app that happened to be using it. The markup contract, the handles, the tokens, the tab mark and the three closing routes are SYSTEM §07; layers §09 keeps what goes INSIDE a drawer for that app, which is a sheet, its margins and the marks on it',
      'REMOVED — the machine page\'s inventory table, and the machine is eleven sections now with its five cross-references renumbered. What the kit already had is a claim about the language, and the language has a page',
      'A PROSE PASS ACROSS ALL FOUR: the machine\'s section intros 1502 words to 414, layers 654 to 392. Nothing was dropped that the demo underneath does not already show',
      'THE MASTHEADS ARE A NAME AND AT MOST ONE LINE. System 4 words, layers 19, machine 25, the whole home 23. A subtitle explaining what a page is for is written for someone who does not own the thing, and nobody who does reads it twice',
      'THE WARNING BLOCKS ARE GONE from all four. Every one of them was declaring a position to an imagined reader: what is untouched, what is a debt, what the entry rule is. The facts that mattered are in version.js, which is where a fact that outlives a page belongs',
      'The em dash was the tell again. Nearly every one was gluing a justification onto a fact the specimen already proves, so cutting them forced the cuts. One survives, in the windows caption, where it is the reduce glyph and not punctuation',
    ],
  },
  {
    version: '1.22.0',
    date: '2026-08-17',
    changes: [
      'SKEW IS THE REPO NOW. The three documents, the kit, the shared stylesheet, the three papers and ref/ came out of targz-skew-design-sytem/ to the root, moved with git mv so the history follows. The home was one folder deep and the launcher had to 302 into it; / serves index.html directly and HOME is back to a single slash',
      'The folder name went with it, and it was misspelled the whole time: targz-skew-design-sytem, no second s in system. A directory nobody could type is one more reason not to have had it',
      'old/ holds the one thing that is actually old. Everything else at the root is either current or infrastructure: version.js spans both eras, BUGS.md tracks a live crash in the layers document, server.js is what Launchpad runs',
      'Removed physical-ui.css.bak, a 98KB copy taken during the August 15 restore. The file it backs up has been in git with full history ever since, which is the same guarantee in a place that cannot go stale',
    ],
  },
  {
    version: '1.21.0',
    date: '2026-08-17',
    changes: [
      'THE REPO HAS A FRONT DOOR: index.html, a home that lists the documents instead of being one. Two explorations and the shipping system were three files you had to already know the names of, and "we may have more pages later" is the condition under which that stops working. One PAGES registry builds the switcher and the cards, so adding a document is adding a line',
      'THE NAV BAR IS ITS OWN SPEC. Section 02 documents the bar at the top of the page by building a second one from the same navBar() function onto a stage. A spec whose specimen is an independent caller of the thing it describes cannot drift from it, which is the failure mode of every component page that draws its examples by hand',
      'AND IT IS AN ASSEMBLY, NOT A NEW PART. .strip was already introduced in physical-ui.css as "the nav bar, a control strip screwed to the desk"; the dividers are .chan, the switcher is .piano, the version window is .lcd. Two things are new: .navdock, the sticky rail, and .nav-mark, which is .demo-etch at nav size',
      'THE DESTINATIONS ARE ANCHORS AND THE PAGE YOU ARE ON IS A SPAN. A .pkey button with an onclick that sets location looks identical and loses middle-click, cmd-click, the status bar preview and copy-link: .pkey is a look, the anchor is the mechanism. And a link to where you already are is a link to nowhere, so it stops being a link and keeps only the travel, the recess and the lamp',
      'THE DOCK CARRIES pointer-events:none because it spans the viewport and the bar does not. Without it a sticky nav is an invisible full-width lid: the top row of every page below stops taking clicks and nothing on screen says why. The strip switches them back on for itself',
      'The seated state is one IntersectionObserver on a 1px sentinel, not a scroll listener: two callbacks in the life of the page instead of sixty a second. The version window reads version.js by dynamic import, because a number written into two files drifts on the first commit that only remembers one, and a failure is then a dim window rather than a dead script',
      'LAUNCHPAD COULD NEVER HAVE RUN THIS. live-server was declared in devDependencies with no node_modules and no lockfile, and the launcher does not install anything: it spawns sh -c with node_modules/.bin on PATH, that directory does not exist, the shell answers "command not found", and startProject returns ok anyway and opens a dead page. It was one npm install away from working on exactly one machine',
      'So the dependency is gone and server.js is zero-dependency: nothing to install, instant start, identical on any checkout. Port precedence is flag, then PORT, then 2860, and both --port 2860 and --port=2860 parse because the launcher rewrites the flag to the = spelling when it assigns one. All four cases tested',
      'THE SHIPPING SYSTEM MOVED TO old/index.html, which is the only genuinely old thing in the repo. version.js is not: it is one changelog across both eras, 1.1.0 to 1.3.0 the light system and 1.4.0 onward Skew. BUGS.md is not either, its open entry is a live crash in the layers document. / is a 302 to the home and not a silent rewrite, so the address bar reads the real path and no URL quietly means two documents',
      'Removed explorations/, two empty directories left behind when physical-ui-layers.html moved out of them at 1.4.0. Added .gitignore for .DS_Store, node_modules and *.bak',
      'Not mine, committed at request with the rest: the SFX master gain, 0.34 to 0.55. It had been cut too far when the voices became impacts and the panel turned into something you had to listen for',
    ],
  },
  {
    version: '1.20.0',
    date: '2026-08-17',
    changes: [
      'A SECOND SKEW DOCUMENT: physical-ui-machine.html, the same language at the plotter. Exploration 01 rebuilt the DRAWING half of targz as hardware; this is the other half, read off TargzPenPlotterCtrl — position strip, jog, pen heights, overrides, run block, alarm banner, job card, port picker, console, status bar. The app is untouched and read-only throughout',
      'THE RESULT IS THE POINT AND IT IS A TABLE: 23 controls, 14 of them free. Panel, tabs, step size, units, the port list, CONNECT, the connection dot, the command line, the utility keys, PARK AT END, the layer dock, the colour swatch, the abort modal and the tool rail all came straight out of the kit at a size. A language is only a language if the second thing you build with it is mostly parts from the first',
      'AND NINE IT DID NOT HAVE, each because no existing primitive could say it: .dro a coordinate under glass, .jog-grid a direction pad, .stick the continuous half of the same job, .penbank a key carrying the height it goes to, .guard a hinged cover, .estop a latching mushroom, .annun an annunciator, .meter a segment progress bar, .rail a read-only status strip',
      'THE KIT CAME OUT OF THE PAGE THAT GREW IT — physical-ui-kit.js: icons, voice bank, key, pkey, knob, fader, rangeFader, drum, rotary, swBtn, picker, typeable, engage, windowise, panelShape, all copied VERBATIM. One line changed: engage reads window.ENGAGE_ROOTS at call time so a second page declares its own. physical-ui-layers.html keeps its inline copy, which is a debt written down in both files rather than paid in a document another hand is in',
      'JOG MOVED HOUSE. The keypad and the stick were section 11 of the layers document with their CSS in physical-ui.css, and nothing in Portrait-Typo jogs anything. They are sections 02 and 03 of the machine page now, whole, rewired to a real machine: the stick deflection is a feed rate in mm/s integrated into position, clamped at the same soft limits the keypad trips, sending one jog command every 250ms instead of 120 a second. physical-ui.css is back to carrying only what both documents use',
      'TWO OPTIONS, TWO SECTIONS. A jog control is only judgeable next to the numbers it moves, so each option is the whole arrangement — readout above control — and the two live in their own numbered sections. It was first two columns on one stage, then a divider and double the gap; both were a layout trying to say what a heading says for free',
      'THE LONE READOUT IS DELETED. A control on its own stage is a specimen: it proves the part exists and says nothing about whether it works, because what has to be judged is the arrangement. The DRO now appears only where it lives, above the control that drives it, twice',
      'A COORDINATE IS A PLACE, NOT A QUANTITY. The travel strip under each number was a fill, and a fill was wrong twice in one row: X and Y at zero drew nothing, so two of three axes looked broken, while Z at zero sat three quarters along its −30…+10 travel and drew a full bar, which reads as nearly finished about something Z is not doing. It is an engraved channel with a stop at each end and a lit pip riding it',
      'THE STICK PIVOTS DEEP, which is the whole answer to a stick that leans but does not move. Travel and tilt are one number, not two: travel = SHAFT × sin(MAX). A pivot 46px under the cap is a stub and a stub can only buy travel by turning, so it ran to 26° and still moved 20px. A real ball is under the PANEL, so the rod is 130px and the limit 9°: same formula, 20px of travel, a third of the rotation',
      'THE CAP HAS A BODY — sixteen discs 2.4px apart in one preserve-3d, each darker and narrower, so tilting turns 38px of barrel wall into view instead of narrowing a coin. The skirt stands proud of the top face so a rim shows all the way round at rest, which is what let the camera come back to dead centre',
      'FIX — up and down were different rotations. The eye was at 24%, far above the socket, and a vanishing point that far off centre makes the two directions different projections: 1.8px of barrel pushing up against 13.5px pushing down. Measured at 43% it was still 6.1 against 9.2. Dead centre is the only value where all four agree, and they agree exactly, at 7.6px each',
      'THE LAMP DOES NOT TURN WITH THE PART. A specular painted into the cap rotates with it, so a face lit from above left spends half its travel lit from below right — the rotary teardrop mistake on a bigger object. The face is relit every frame against a fixed lamp: the hot spot slides across it and the whole face gains and loses light with its angle',
      'AND THE BALL UNDER IT IS NOT RELIT, WHICH TOOK TWO PASSES TO GET BACK TO. A rotating sphere shows no shading change — terminator and hot spot are fixed by the LAMP, only surface features move, and a polished dome has none. Dragging its gradient is the one thing that cannot happen, and the eye reads the impossible version as the surface deforming. The attachment reads through travel instead: the dome swings a third of the cap',
      'IT IS A BALL, NOT A SHADOW. The dark ring under the cap was first a near-black hole and then a blurred ellipse laid over the well, both the same misreading. A cast shadow is darkest nearest what casts it; a sphere is bright top-left, falls through a terminator and picks up a cool bounce off the socket floor. And a sphere has NO OUTLINE — the 1px ring round it was the collar, sitting exactly on the rim, so a machined edge and a sphere shared one contour and the sharp one won',
      'THE SOCKET SHADOW IS FIXED, because it belongs to the hole. It was an inset shadow on the dome, so once the dome tipped the shadow tipped with it — and a shadow cast by a hole that travels with the thing inside it is the one cue that says there is no hole',
      'ABORT GOES UNDER A COVER, the only genuinely new mechanism on the page: two deliberate actions, the first reversible, the second impossible until the first has happened, and it shuts itself after six seconds the way a sprung one does. Colour is not a guard — it was one pixel of pointer travel from the two keys pressed fifty times a session',
      'THE MUSHROOM IS MOUNTED, NOT POSTED. It was 104px on a yellow hazard plate with EMERGENCY STOP across the cap: a real cap carries no text, the stripes belong to an enclosure door, and at twice the size of anything else it read as loud rather than important. 58px in a machined collar, legend cut into the panel beside it',
      'THE ANNUNCIATOR REPLACES THE ALARM BANNER. A machine does not have one alarm, it has a dozen inputs, and the question when it halts is WHICH — a sentence answers that in prose, one at a time, and only while it is showing. Eight legends, engraved and dark permanently, so the panel is a map you learnt before you needed it. New alarms flash; UNLOCK acknowledges, and a held E-stop survives it',
      'FIX — the flash was a strobe. 1.6Hz, hard edges, half the cycle at a third brightness is the honest transcription of a real annunciator and does not survive being put on a 46px cell of saturated orange. The rhythm carries the meaning, not the depth: same hard edge at 0.8Hz, a short dip to 62%, and it stops after ten cycles and holds lit',
      'PROGRESS IS SEGMENTS, because the question at 2am is whether it is still moving and a fill creeping 0.3% a minute is indistinguishable from a fill that stopped. Forty of them, the arriving one hot, with the percentage in its own glass beside it',
      'THE LOG IS A ROLL, not a scrolling div — newest at the tear edge, older darker because it is further into the machine, and the one place on the panel where colour is data: sent lines in the phosphor, replies dimmer, errors red',
      'EVERYTHING IS WIRED TO EVERYTHING. One state object: jog writes position, position writes the rail, run writes the meter, the annunciator kills run, and every motion prints G-code on the tape and counts the queue. A panel of controls that do not drive each other is a picture of a panel',
      'FIX — letting go outside left the stick at full deflection with the axes still counting up. Pointer capture is lost on an iframe, a context menu, the window blurring, a gesture the browser decides is a scroll. Release is caught in four more places and is idempotent. A jog input that keeps commanding motion after your hand is off it is the failure that matters',
      'HOME IS A HOUSE. It was ICON.settle, an arrow dropping onto a line, which is the mark for park; then the axis triad with three heads turned in to face the origin, which is the correct drawing of what the key does and the wrong drawing for the most-pressed cap on the panel — six marks converging on one point at 15px. The most-pressed cap is the one you must never look at twice',
    ],
  },
  {
    version: '1.19.0',
    date: '2026-08-17',
    changes: [
      'THE SETTINGS PANES MOVED INTO AN OFF-CANVAS DRAWER, push variant. Off-canvas names where a thing lives when it is shut, which is the useful fact about it: an OVERLAY slides over the page, a REVEAL slides the page off something underneath, and a PUSH moves the page aside. Push is the one worth building — an overlay hides the drawing at the exact moment you adjust it, so you slide it out, lose sight of the thing, change a number and only find out what you did after sliding it back',
      'THE TAB RIDES ON THE DRAWER, so when the drawer is away the tab is the one bump of it still showing. A control floating in the corner would be a handle that stays put while its drawer moves, which is a handle for nothing. The whole head is the grab area — a title bar is the one strip with nothing in it you can hit by accident',
      'ONE CONTROL, AND THE MARK ON IT IS THE STATE. Shut it carries three sliders, which is what is behind it; open the tracks fold into a cross and the handles go, because what the same press will now do is close. It was briefly two — a tab and an × in the drawer head — which is two buttons a thumb apart closing the same thing when the top one had to be there anyway as the thing that opened it',
      'A BURGER SAYS MENU AND THIS IS NOT ONE. Three equal bars is the mark for a list of destinations; behind this tab are knobs, switches and a paper size. Sliders say it as a picture of what is in there rather than as a convention you have to have learned',
      'FIX — the cross came out as a chevron. The three tracks were block elements with a margin, so folding them meant guessing how far apart their centres were, and a guess out by a pixel gives two arms at the right ANGLE on the wrong LINE. Placed absolutely they land on the same line and cross at a point',
      'CLICK ANYWHERE OUTSIDE CLOSES IT, because going back to what you were doing IS the instruction to put it away — a drawer that makes you dismiss it first and then repeat the click you already made is charging you twice for one decision. It is captured POINTERDOWN and not CLICK: a control that rebuilds its row on release leaves the pointer on a detached node, so by the time click fires the inside/outside test is being asked of something already thrown away',
      'Exempt from that: the drawer, the picker (its plate is a child of body, so a swatch\'s own field reads as outside and the drawer would eat the popup it opened) and the section 09 specimen (or pressing it would close and reopen in one gesture)',
      'NO PANEL INSIDE A PANEL. The two panes were .box elements, so each brought its own bevel, grain and cast shadow onto a slab that already had all three — two chassis deep and the wells stop reading as cut into anything, because the thing they are cut into is itself floating. The groups lost their case and kept their contents: the bays are still milled wells, and that was always the part doing the work',
      'FIX — the dials hung off the left edge. --rcx is the DIAL\'S CENTRE and the dial is 54px, so anything under 27 puts its left half outside its container. It had been 22 the whole time and nobody saw, because the .box padding was absorbing the overhang; stripping the chassis took that away and the overflow became visible. Second time today a value was propped up by something unrelated',
      'And the switch is centred as a GROUP rather than nudged. The rotary filled its cell and placed the dial absolutely, so a half-width bay left the knob against one edge and a column of air against the other. The container now takes the width of what is in it — dial, leader, name — and auto margins do the rest, which stays correct at any bay width',
      'SECTION 09 IS A SPEC, NOT AN ESSAY. It was four paragraphs of prose about a drawer; it is nine reference rows: markup contract, handles, tokens, z-index scale, the mark, and the closing routes, with the three a dev would otherwise get wrong marked in amber. 520 words to 188',
      'AND THE SAME PASS ACROSS ALL TWELVE SECTIONS — 820 words to 632, and 19 em dashes to none. The dash was the tell: nearly every one was gluing a justification onto a fact the demo underneath already shows. Cutting them forced the cuts. Nothing was dropped — every claim that only lived in prose is still there',
      'SECTION 10 opens with all three resting states on screen at once: one panel open, one reduced to its title bar, one closed with its chip lit. A demo that opens with everything open shows you what a panel looks like, which you already knew; what you cannot see anywhere else is what AWAY looks like, and away is two different things here',
      'Plus an OFF-CANVAS model inside one of those windows, because a window moves and gets out of the way while a drawer never moves and is only ever in or out. Both answer "this is in my way", and the useful thing is seeing which question each one answers. Deliberately the wrong proportions: at the real ratio the page barely shifts and the demonstration demonstrates nothing',
      'SECTION 08 HALVED, and 1:1 survives because the millimetre did not move. The sheets are 34.4mm and 42.3mm instead of 68.8 and 84.7, so the ball is half the ball, but the pen is still 0.35 and the pitch still 2.17 because those are properties of the PLOTTER. What is lost is passes. Scaling the rendered panel instead would have kept every stroke and cost the 1:1, which is the wrong trade on a page whose argument is that these are real sizes',
      'It also stopped breaking its banks. At full size the pair was 1260px against a 1160px wrap so the stage ran to the viewport edge and scrolled sideways; halved it is about 700 and goes back inside, centred. A section should break the grid because it cannot fit, not because it once could not',
      'REMOVED — the pale comparison panel in 01, its builder and its 23 CSS rules. Section 08 carried a note explaining that `.hh-shape` was named that BECAUSE `.flat` was taken, with the story of the white border it inherited; that claim is now false, so it says the name is free and keeps the lesson. A comment describing a class that no longer exists is worse than no comment',
      'The second pane is DROP, not SETTINGS. The drawer is called SETTINGS so a group inside it cannot be, and it cannot be LETTERS either because one of its own bays is',
    ],
  },
  {
    version: '1.18.0',
    date: '2026-08-16',
    changes: [
      'TWO NEW SECTIONS. 09 rebuilds a whole Tweakpane — SETTINGS and CANVAS — with nothing new invented, because the work of that panel is not drawing, it is CHOOSING: a pane of sliders and dropdowns has only two kinds of thing in it, so every parameter looks like every other one and the panel tells you nothing about what you are setting',
      'THE FIVE DECISIONS it comes down to: a QUANTITY is a knob; a SHORT LIST is a switch; a LONG LIST is a drum; TWO EXCLUSIVE THINGS are two keys that cannot both be down; and a number the app WORKS OUT is a readout, not an input you are invited to fight',
      'A FOLDER IS A BAY, not a collapsible header. Five folders that all collapse is a panel that has hidden itself — you open one to look and close it to reach the next, and at no point can you see the machine. Only ADVANCED gets a chevron, because what it hides is genuinely rare: four numbers you set when the simulation misbehaves and then never touch again',
      'A SEED IS NOT A QUANTITY, so it does not get a knob. You do not sweep a seed looking for a value — 41 and 43 are no more alike than 41 and 9000. It is an IDENTITY: a lit window you can type an exact one into, and a key that throws a new one. A slider there is the one control on a panel where dragging is genuinely meaningless',
      'SQUARE IS AN ORIENTATION, NOT A FORMAT. It shipped in the format list, which put it in the wrong argument entirely: A2 is a quantity of paper and portrait is what you do with it. Squaring an A2 is still an A2 — you have cut it down — so it joined the interlock, and changing format while square re-fits the side to the biggest square the new sheet gives',
      'WIDTH, HEIGHT AND MARGIN ARE THREE KNOBS ON ONE LINE, always. They were two lit windows, a Side knob and a pair of Custom knobs swapping in and out of one slot — three layouts for three states, so the panel changed SHAPE as you worked, and a control that moves house depending on the mode is one you have to find again every time',
      'The knobs are always there and the MODE changes what they do: on a named format they REPORT, and turning one says the sheet is something else, so the format rolls to Custom and keeps your number. The act of turning is the act of claiming — which is how the readout rule survives rather than breaks. On Square they are tied, because two knobs that could disagree about one dimension offer you a way to make a square that is not one',
      'Write-back is SILENT. .set() fires onChange and onChange claims Custom, so a plain sync would turn every format change into a claim and make the format list unusable',
      'Any lit window takes typing now, not just knob readouts — the .kedit styles were scoped to .kval, and a lit window is a lit window',
      'SECTION 10 — WINDOWS. Every panel moves, and every panel gets out of the way EXACTLY ONE WAY. Close or reduce, never both: both buttons answer the same question, so a title bar with both asks you to choose between two ways of doing one thing, every time, forever',
      'And the choice is not the user\'s, it belongs to the PANEL. One you can do without CLOSES, and something has to bring it back. One the app needs REDUCES, because closing it would strand something essential. The button is the panel telling you whether it is optional',
      'THE WHOLE HEAD IS THE HANDLE — a title bar is the one strip with nothing in it you can hit by accident, which is why every window in every OS drags from there and why adding a separate grip is inventing a control the object already has. Dragging is a transform and not left/top, so a panel crossing a page of live specimens does not reflow them sixty times a second, and the one you grab comes to the front',
      'REDUCED IS THE TITLE BAR AND NOTHING ELSE, not a shrunken panel — a panel at 50% is one you can neither read nor use. The dock is the price of being closeable: a menu three levels down is how a closed panel becomes a lost one. The tab strip and the footer are out of scope on purpose, because those are the chassis and a page whose own navigation can be dragged into a corner is a page you can lose',
      'rotary() takes `compact`, for a switch that is one of five on a 318px panel rather than the one object in a viewport. Nothing about the switch changes — same fan, same detents, same leaders — only the space it reserves: names 4px closer, leaders pulled in 8px, and a floor from the seat rather than a constant. It had been reserving 130px for a 54px dial and four names, which is 42px of empty per selector',
      'And the copy gave way where it had to: "Same as view" and "Last drop" were the only two names that could not share a row, and a column of empty floor is a steep price for four letters. What did NOT give way is which primitive it is — shortening a name is a copy edit, turning a switch into a dropdown to save the same space would be the panel losing the argument',
      'The format rows carry their size — A4 · 21.0 × 29.7. "A4" alone is a name you have to already know, and the row was mostly empty AND mostly useless at once',
      'RENAMED — the panel is SETTINGS, not LETTERS: a panel and a folder inside it cannot share a name without one being read as the other. And the second is CANVAS, not GLOBAL: "global" says where a setting REACHES, which is a fact about scope and not about what the panel is for',
    ],
  },
  {
    version: '1.17.0',
    date: '2026-08-16',
    changes: [
      'THE WHEEL ROLLS WITH THE DRUM. It was a still picture of a knurl beside a list that moved, which is the one thing a wheel must not be — the surface travels R·θ per detent, 17.9px at an 18px pitch and 18°, so the teeth now move exactly as far as the names do, on the same spring the names settle on. A wheel and the thing it drives have to agree or neither is believable',
      'ONLY THE TEETH MOVE. Two background layers and the split is the whole point: the knurl is a property of the CYLINDER and travels with it, the crown highlight is where the LAMP falls and stays put however far the thing has turned. Slide both and it stops being a wheel and becomes a conveyor belt',
      'THE TOOTH PITCH IS 3.2px AND NOT 3px, on purpose. One detent rolls the surface 17.85px, which at a 3px pitch is very nearly six whole teeth — the pattern would land identical every time, so the only movement you could ever see was during the tween and a wheel turned five times would look untouched. At 3.2 it crosses 5.58 teeth and lands 1.85px out, two thirds of a tooth, so the motion reads AND the resting state remembers it',
    ],
  },
  {
    version: '1.16.0',
    date: '2026-08-16',
    changes: [
      'NEW PRIMITIVE — the DRUM, for a list the dial cannot hold. The rotary\'s fan is a fixed 80°, so its detents run from 27° apart at four names to 9° at ten, and a real selector switch detents at about 30° — the positions stop being AIMABLE long before the names stop fitting. Six is the ceiling and the 3D page had already passed it',
      'It gives up the rotary\'s claim to show every position, and gives it up honestly. A drum shows the NEIGHBOURHOOD — the one you are on, lit, in a milled window, with the names either side curving away. A <select> is a list that HIDES; a drum is a list you are looking at part of, and the part you cannot see is visibly still there, rolling round the back of the cylinder',
      'IT IS A REAL CYLINDER, not a scaled list: each name is rotated about the drum\'s axis and pushed out to its radius in 3D, so the spacing compresses towards the edges and the top and bottom rows FALL AWAY rather than fading. A list with an opacity ramp on it is a list with an opacity ramp on it, and the eye knows',
      'The geometry is solved rather than guessed — a name k steps out projects to R·sin(kθ), so how many you SEE is fixed by the window height and not by how many you draw. 18° at an 18px pitch gives a 57px radius, which puts the fifth name past a 76px window: five visible, the sixth clipped, the right neighbourhood for a list of ten',
      'FIX — the first build magnified everything. Items sat at translateZ(+R), 57px toward the viewer against a 200px perspective, so the front row was scaled 1.4× and every name came out sliced down both sides. The whole drum is pushed back by its own radius now, so the row you are reading lands at z=0 at its true size',
      'FIX — you turn it by the WHEEL, not by the names. Dragging across the window was dragging across TEXT, so the browser started selecting it: a blue smear over the list with the drum moving underneath. The names are not a handle, they are the thing being read. A click still picks a name, because a click is not a drag',
      'THE WHEEL WAS SHADED THE WRONG WAY ROUND. The drum\'s axis is horizontal, so its edge turns away at the TOP and BOTTOM and is broadest across the middle — the shading has to run vertically to match, and it ran horizontally, which shades a cylinder lying the other way and contradicts the very list rolling beside it. 24px wide now, lit crown just above centre, and the teeth die at the ends because on a real wheel they crowd together as the surface turns out of sight',
      'THE WHEEL COMES UP THROUGH A CUT. It was butted against the window, which reads as two panels side by side rather than a cylinder in a slot: hard dark walls closing over it, the far wall catching the room at the bottom, a chamfer on the cut edge, and a lit line on the CASE ABOVE the opening — a recess is dark inside its top wall, but the flat face it is cut into is the part facing the lamp most directly, and without that line the drum had no top at all',
      'AND THE WHOLE ASSEMBLY DROPS INTO A MILLED WELL with 8px of floor showing all round. A recess is read from its FLOOR, not its walls, so a well with no floor showing is not a well, it is a border',
      'BLOCKED BETWEEN NOTCHES, on purpose. A mouse click and a trackpad flick are not the same event: a wheel sends one big delta per notch, a trackpad a stream of small ones, so accumulating everything fired twice per mouse click and stepping per event ran the list past. A big delta IS a notch and lands at once; small ones are added up until they are worth one — and both pass a 70ms lockout, which is about as fast as a thumb can index a real wheel. The lockout is on the GESTURE and not on set(), because a tab switch writing six controls must land immediately and silently',
      'AND THE DIRECTION IS INVERTED, because you are turning a cylinder rather than scrolling a list. Push down on the face of a wheel and its top rolls away from you, so the names come UP past the window. Matching the scrollbar convention would mean the drum turning the opposite way to the thumb on it',
      'THE DETENT MAKES A NOISE, because a detent IS a noise. This is the one control whose defining feature — the notch it drops into — has no visual at all: you feel a detent, and failing that you hear it. Synthesised rather than sampled, a 40ms chirp falling 2.2kHz to 700Hz through an exponential gain, built lazily on the first turn because an AudioContext made before a gesture is one the browser suspends. It fails silently where audio is blocked and the drum still works',
      'Plus the two visual halves of a notch: the name OVERSHOOTS and settles, because one that slides to its place has been dragged there and one that goes slightly past has DROPPED INTO something — and the case takes the knock, the detent line brightening for 90ms',
      'FIX — the rotary\'s box grows with its list. It was a fixed 130px, which fits six names at 22px apart and silently clipped the seventh; the column is centred, so the overflow went UP as well as down and the first name landed on the caption above the bay. Growing the box does not raise the ceiling — the detents are still the limit — it just stops the failure being invisible',
    ],
  },
  {
    version: '1.15.0',
    date: '2026-08-16',
    changes: [
      'A STRUCK LEGEND, NOT A FADED ONE, on a surface that is not drawn. Dimming says "this key is unavailable", which is the opposite of true — an undrawn surface is still fully editable and you often want to set one up BEFORE switching it on. A strike says the name is still there and the MARK IS NOT: the proof-reader\'s gesture, and the only one that means removed rather than disabled. The line takes the key\'s own colour, so an off tab that is down strikes in the lamp colour and one that is up strikes in grey',
      'THE LAMPS GO OUT on a surface that is not drawn. Every lit thing on the bay means one thing — this is going to be plotted — and on a struck surface none of it is true. ONE FILTER, NOT TWENTY OVERRIDES: the lamps are spread across knob arcs, fader fills, cap indicators, readouts and the ink lamp, and half of them hard-code their colour, so hunting them one by one would miss some and go stale. The controls stay LIVE and only their light goes, and the tab strip is excluded, because you have to be able to reach the tab that turns it back on',
      'FIX — STROKE COULD NOT BE SWITCHED OFF. It was given no state at all, on the reasoning that a contour has no hatch: true, and it does not follow. A contour is still a MARK — it has a pen, it has an ink, and you can decide not to draw it. Leaving it stateless made the one surface on the strip that could not be turned off, which is not a rule anybody would have written down',
      'FIX — AND STROKE HAD NO PREVIEW, for the same bad reason. The surface has no FILL, which is not the same as having no MARK: what a contour plots is the outline at this pen and this ink, and that is exactly what you go to the tab to set. Both sheets draw it now — the ball\'s silhouette, and both of the blob\'s edges, because anything that draws the boundary draws two of them or it is drawing a lie',
      'And `hatches:false` now dims only the four controls that fill an AREA. Pen and ink stay live, because a contour has both — dimming the whole row made the one tab where you set a line\'s weight the one tab where you could not touch anything',
      'FOLLOW THE OBJECT TAKES THE ANGLE KNOB OVER rather than reinterpreting it. An earlier build kept the knob live and made it an OFFSET from the object, which works and asks you to hold two frames of reference in your head to read one number. Two controls steering one value is a panel where whichever you touched last wins and neither says so — so the knob goes dim, and the toggle is not a seventh setting but who decides the sixth',
      'ONE GLYPH FOR FOLLOW, not two. It had a generic box icon on the switch and a separate engraved protractor beside it: two marks competing to explain one toggle and neither saying "the hatch turns with the shape". Now a tilted body with its fill tilted to match — one shape, three strokes inside it, same angle — at 15px in a 26px cap, because it is the only mark on that row',
      'THE INK RAIL IS NINE IDENTICAL SLOTS AND ONE LAMP. The unselected caps were as proud as the one in hand and only a light apart from it, so the rail read as nine equally-live pens and the eye had to hunt. They sit down in the case now: the recess recipe with all four hairlines inverted — hard near-black inside the top, the wall shading from it, a faint light inside the BOTTOM because that is the wall the room reaches, a lit lip outside below, and no cast shadow at all, because a hole does not throw one. Plus the directional chamfer every seated control here gets',
      'AND SELECTION IS THE LAMP, AND ONLY THE LAMP. Three other signals were tried and all three were redundant: the selected cap was bigger, brighter, and its core was inverted. Each is a second thing to notice that means the thing you already noticed, and the cost is not clutter — it is that the rail stops being a row of nine LIKE OBJECTS. A pen case is nine identical slots; which pen is in your hand is a fact about you, not about the slot',
      'The dim on the unselected is a FILTER rather than a second set of colours: nine inks would need nine muted twins, and they would drift from the real ones the first time either was tuned',
      'THE SWATCH THAT OPENS THE PICKER NOW CLOSES IT. Every other way out was already there — Escape, a click anywhere else — except the one the hand reaches for first, which is the thing you just clicked. Without it, clicking again tore the plate down and built an identical one in the same place: a control that visibly does nothing, which is worse than one that does nothing quietly, because you try it twice',
      'ADOPTING-SKEW-1.14.md — what an app already on Skew has to pick up from v1.9.0 to v1.14.0. Not a code drop: every section says what to copy, what it depends on and what it costs, so a team can take three things and skip the rest. Four new primitives, the additive factory API, six behaviour patterns written with the wrong version first because that is the part that transfers, and six traps — substring selectors, the background shorthand, clip-path eating box-shadow, canvases having no pseudo-elements, rebuilding a list from inside an open popup\'s callback, and SVG inner-group rotation',
    ],
  },
  {
    version: '1.14.0',
    date: '2026-08-16',
    changes: [
      'SIX SURFACES ON A TAB STRIP, above the specimen. A drawing is not one fill: the face of a letter, its sides, its shadow and the ground under it are four different marks, and a panel offering one set of hatch settings makes you believe they share them. Each tab carries its own type, pen, spacing, angle, ink and follow — and switching loads all of it, which is the entire reason knob(), fader() and now rotary() expose .set(). A strip that changed a heading and left six controls showing the previous surface\'s numbers would be worse than no strip',
      'THE TABS GO ABOVE THE SPECIMEN, not below it. They are not a control the way the knobs are — they choose WHICH DRAWING the bay is about, and a selector under the thing it selects reads as belonging to it. Everything below the strip is the surface the strip picked',
      'EDIT ALL, on the same bar past a milled channel. It replaced an enable switch that was answering a question nobody asked at the end of a strip about WHICH surface — the useful question there is the opposite one: whether the next change lands on the surface you are on or on all six. Six surfaces means six of every setting, and without it you edit the pen six times and the sixth differs from the first because you mistyped it',
      'ALL spreads ONLY THE FIELD YOU TOUCHED. Copying the whole state would make it mean "make them identical", which is a different and far more destructive thing than "change this one setting everywhere". And it is a LATCH, held down while on, so a knob turned by accident with it engaged is a mistake you can see before you make it',
      'With ALL engaged every tab goes down and lights. Dimming the five you are not on said the opposite of the truth — that they were less involved — when the point is that a change now lands on all six equally. aria-checked still names exactly one, because DOWN is what the change reaches and CHECKED is what the controls are reading, and those stop being the same fact the moment ALL is on',
      'TWO FACTS, TWO GESTURES, TWO CHANNELS. Which surface you are EDITING and whether it is DRAWN are different questions, and the first build ran both through one switch and got both wrong: turning a surface off also meant leaving it, so you could not see what you had turned off and coming back to look turned it on again. Single click selects, double click toggles drawn. DOWN is what the next change reaches; a LIT LEGEND is what gets plotted. A tab can be down and dark — editing something that will not be drawn, which is a real and useful state — or up and lit',
      'And with ALL engaged the double click goes wide too. Edits spreading but on/off not is the kind of half-rule nobody can hold in their head, and the first surface it surprises is the one you stopped looking at',
      'FOLLOW THE OBJECT — optional, and it changes what the Angle knob MEANS. Off, 45° is 45° from the sheet. On, it is 45° from the object, so a letter\'s sides stay lit from the same side however it is rotated and a shadow keeps running across the light instead of drifting into a pattern that fights it. It sits next to Angle and nowhere else, because it is not a seventh setting — it is the units the sixth is measured in, and a toggle that changes what a number means from across the panel is two controls you have to remember are connected. The readout says which: 45° absolute, +45° relative',
      'ALL is a WORD again. As a glyph it was a quarter of the width and cost more than it saved: every other key on that bar is a word, so a lone mark among six names reads as a different KIND of thing rather than the same kind with a wider reach, which is the one thing it must not say',
      'REVERTED — glass shading on the ink swatches. Bright walls, a hard specular and the ink as a core is the correct recipe for a tube and the wrong one for a 28px square, because at that size the two lit walls and the specular are most of the object and the colour is what is left over. On a swatch the COLOUR IS THE POINT, and any treatment that spends the width describing the material has already lost. Recorded in the CSS so the dead end is only walked once',
    ],
  },
  {
    version: '1.13.0',
    date: '2026-08-16',
    changes: [
      'THE ROTARY\'S LEVER IS A KNOB NOW, and the roundness is an argument rather than a style. It was a white teardrop, which has to rotate as a whole — so its lighting rotated with it and "lit from above left" spent half the dial lit from below right. Counter-rotating the shading inside the silhouette fixes the physics and still looks wrong, because the eye reads a moving edge as a moving surface whatever is painted on it. A circle has no orientation, so the cap never turns, its lighting is nailed to the room like every other cap here, and the only thing that moves is the indicator',
      'Four rings and a dot, built outside in: a knurled collar, a lit bevel, a near-flat face, one small bright dot. THE FACE IS ALMOST FLAT IN VALUE and that is the whole difference between machined metal and a plastic bead — a broad radial sheen says the surface is domed and slightly wet, and a real knob\'s top is a turned disc. All the light lives in the edges',
      'Each knurl tooth takes the lamp separately, bright where it faces it and dark where it turns away. That alternation IS the knurl; a uniform ring of ticks is a texture, and textures do not turn in the light',
      'THE INDICATOR IS A DOT, NOT A LINE. A line is a second radius competing with the ring it sits inside; a dot is a position and nothing else, which is all a pointer has ever had to be. 62px, down from the first round build',
      'GLASS SHADING ON THE INK SWATCHES — the same squircle, but it stops being a painted face and starts being a body of ink seen THROUGH something. The walls are the brightest thing on it, which is the counter-intuitive half: the instinct is to darken the edges because that is more colour to look through, true of a solid lump of liquid and false of anything with a WALL. A lit glass wall throws the room straight back, so both edges go white and the colour retreats to the middle',
      'One hard specular left of centre running the full height, because it is on the OUTSIDE of the glass and belongs to the room rather than to the ink. The seated state restates every layer rather than half of one: `background` resets background-image, so declaring only the core would strip the walls and the specular off the one swatch you are looking at — the trap that once erased a knurl. Only the CORE inverts; the room does not move',
      'ANY LIT READOUT TAKES A TYPED VALUE — double-click it. A knob is for LOOKING FOR a value and a keyboard is for KNOWING one, and a panel that only offers the first is one you fight whenever you already have the number. Pen, Spacing, Angle, Count, Attempts, Yaw, Pitch, Roll, Hue, Min stroke, Join reach, Level and Trim',
      'Double-click and not click, because a single click on a readout is what happens on the way to somewhere else and a field that opens by accident eats the next keystroke. Units are formatting, not input — 45 and 45° both work. The window does not become a form control: same lit glass, same phosphor, same tabular figures, a caret and nothing else, and NO I-BEAM on hover, because a text cursor claims it is a text field and it is not — it is a display that will take a value if you ask it twice',
      'The control underneath stops hearing arrow keys while the field is open, or typing into the box also nudges the thing the box reports on. The write happens after the input is removed, because write() repaints the readout and a repaint with the input still in it leaves the field half-torn',
      'FEATURE REQUEST recorded, not built: the ink swatch as a glass capsule, filled or empty. Written up with the prior art — pen cap, nib and pipette were all built and rejected before the squircle, and all three failed the same way, by getting better at being a picture of a thing on the desk and no better at being a control. And with the open question it has to settle first: a slot is binary today, so a three-state capsule needs a third fact to carry or half-full is decoration',
    ],
  },
  {
    version: '1.12.2',
    date: '2026-08-15',
    changes: [
      'PROSE CUT THROUGHOUT physical-ui-layers.html. The page had grown an essay per section — three lede paragraphs where one does the job, and section 07 running to five paragraphs describing a control that is on screen, live, two inches below the sentence describing it',
      'The rule applied: if the demo shows it, do not write it. A dial with six visible positions does not need its six positions listed; a knob with a lit arc does not need the arc described. What survives is the part the picture cannot say — why a ball and not a swatch, why the blob is not optional, why the three knobs stay independent',
      'Section 07 from ~40 lines to 11. Masthead 3 paragraphs to 2, the warning banner halved, sections 01–06 and 08 cut to two or three sentences each, both bill columns tightened, and the fifteen material tiles trimmed of their restatements',
      'No markup, no control and no behaviour touched — text only. Script parses, div count unchanged at 44, mat-count still writes itself',
    ],
  },
  {
    version: '1.12.1',
    date: '2026-08-15',
    changes: [
      'HATCH-API.md — the reference for the fill generators, written because the section that documented them did it in eleven paragraphs of prose. That prose was the right thing to HAVE and the wrong thing to put in front of somebody who wants to use the code: the argument for a choice and the interface to it are two different documents, and only one of them belongs on the page',
      'THE ONE CONTRACT, stated once and in one place: a region is a function tone(x, y) — negative outside, 0…1 inside, 0 bare paper and 1 as dark as the fill goes. That is the entire coupling between what is drawn and how it is filled, which is why any subject that can answer it can be filled by any generator here and no generator ever asks what shape it is on',
      'And the things you only find out on paper: everything is millimetres at 96/25.4 with no pixel below the DOM layer; pitch and nib are ONE setting; four passes at pitch s land the darkest tone at s/4, so s stays four nibs clear of the flood crossing; dither is not optional on a hand fill or the tone bands into isolines of the lighting; the pole trap in latitudes and why aiming the axis down the light hides both blank caps',
      'THE WALL WALK IS WRITTEN UP AS DELIBERATELY NOT IMPLEMENTED, with the reason, so that nobody rebuilds it. It takes a flat fill to one stroke and it was removed — the count is not the drawing, and every trick of that kind buys a lift with a line that cannot be erased. A dead end is only recorded once; after that it is just an absence somebody helpfully fills in again',
      'The page keeps the ultra-basic — the contract, six signatures, one line each — and a door to the file. The reasoning went where it belongs, into the code comments beside the line each paragraph explains, where it cannot go stale without the line going with it',
    ],
  },
  {
    version: '1.12.0',
    date: '2026-08-15',
    changes: [
      'FIX — SERPENTINE WAS NOT SERPENTINE, and it joined in the wrong place twice over. It chained the runs WITHIN one scanline and never joined one scanline to the next: on the ball every scanline has exactly one run, so there was nothing to chain and the result was PARALLEL under another name',
      'FIX — and on the blob, a scanline crossing the hole has two runs, so joining them ruled a line straight through the hole. That is why the shape came out solid. A serpentine is a boustrophedon: the join belongs at the TURNAROUND, end of one pass to start of the next, and a gap inside a pass is a hole, which is exactly where a pen DOES lift. Between lines, connect; within a line, lift. Measured after: 7 movetos for 73 linetos where parallel needs one per line, and zero ink inside the hole on all three machine fills',
      'THE DIAL CARRIES BOTH WORLDS — three machine fills (Parallel, Crossed, Serpentine) and section 08\'s three hands (Hand, Hand cross, Hand random), borrowed rather than reimplemented: a second implementation of a hand would be two hands that drift apart, which is the same argument KIT makes about the ball. One definition, two callers. The bay\'s old machine-made Hand hatched is retired — it broke the register, and noisy is not hand-made',
      'TWO SPECIMENS SIDE BY SIDE, ball left and blob right, and neither is optional. The ball answers what TONE a fill makes; the blob answers what it does when it hits an EDGE — where strokes stop, and what happens in a notch too narrow to fit one in. A fill can pass one and fail the other, so one sheet would let a hand look finished when it cannot turn a corner',
      'And both subjects come from the SHARED KIT now. This section used to light its own sphere, which is the one thing the kit exists to prevent: light the ball differently in 07 than in 08 and every comparison between a machine fill and a hand one is comparing two balls instead of two fills',
      'ALL THREE KNOBS REACH THE HANDS. They did not at first, on the reasoning that a hand\'s pitch and angle ARE the hand — wrong in an instructive way: 08 writes every hand against a BASE, each pitch a multiple of it and each pass angle an offset from it, so the character lives in the relationships and the base is only where they are anchored. Moving the anchor rotates the whole fill or changes its pitch without changing what it looks like, which is the difference between a parameter and a different drawing',
      'Spacing is passed only when it means something: the hands default their pitch FROM THE NIB, so a caller that picks up a bigger pen and says nothing gets the same drawing with a bigger pen rather than a darker one. Handing them a number unconditionally would take that over on every render',
      'One documented dead end survives: hand random has no register to rotate — 08 says a fill with no readable direction is the whole definition of it — so Angle goes quiet on that one position rather than turning and changing nothing. Verified by measurement: spacing moves all three hands, angle moves two of the three and leaves random byte-identical',
    ],
  },
  {
    version: '1.11.0',
    date: '2026-08-15',
    changes: [
      'ONE SECTION, TWO HALVES. 08 is now FILLING — by hand, and by machine — because the two blocks want opposite things from the same job and that is only legible if they are read against each other. They share a rig, so the ball, the lamp and the blob are identical on both sides; the panels sit in one stage with a gap between them. Materials is 09 and the bill is 10',
      'BY MACHINE — THE SERPENTINE, the second half, and it takes the machine\'s side completely: no wobble, no jitter, no drift, exact pitch, straight lines. What it optimises is not the picture, it is THE PEN LIFT — a plotter\'s real cost is not line length, the head moves at the same speed whether the pen is down or up. It is the number of times the pen comes out of the paper, aims, and comes back down, and every one is a chance to blot and to leave the comma a felt tip makes when it lands',
      '3D PRINTERS SOLVED THIS FIRST and the answer is the same in two dimensions: at the end of a pass do not lift, TURN. The next pass runs backwards so its start is where the last one ended, and the two are one stroke with a corner in it',
      'A CONTROL AND A RESULT, side by side, which is the only honest way to show it: left column lifts at the end of every pass, right column turns instead. Same tone model, same pitch, same nib, same drawing, and a lit readout under each — the two numbers are the argument, and they are read off the panel rather than quoted here, because a number in a changelog goes stale the first time the pitch moves',
      'THE COUNT IS NOT THE DRAWING, and this is what the half is actually for. There is a version that gets the flat shape to ONE stroke: route along the outline where the straight cut runs out, then carry on round the perimeter. It works, the readout says 1, it was built — and it is GONE. Every trick of that kind buys a lift with a LINE, and a line is permanent: the wall inks twice where the pen retraces it, and the closing join reaches from wherever the fill happened to stop to wherever the perimeter happened to start, straight across the middle of the shape. One of those ruins a plot. Sixty pen lifts do not',
      'SO THE RULE IS THE CONSERVATIVE ONE: turn where the next pass is genuinely next to you, lift otherwise. A SERPENTINE IS A FILL THAT DOES NOT LIFT WHEN IT DOES NOT HAVE TO, NOT A FILL THAT NEVER LIFTS — and chasing the second is how you draw the first line you cannot erase. Lifting is free; ink is not',
      'THE BLOB HAS A HOLE IN IT NOW, on both halves, because a fill that has never met one has not been tested. Every hard thing about a boundary happens twice as often once the region has an inside edge: passes break in the MIDDLE instead of at the ends, the two halves of a broken pass stop being neighbours, and any join reaching across the gap draws a line through the one place on the sheet meant to stay paper. A fill is not proved by the shape it fills, it is proved by the shape it REFUSES to',
      'AND SHRINK-UNTIL-IT-FITS IS NOT A PLACEMENT. It terminates every time and sometimes terminates at a hole a third of a millimetre across, which is a dot the fill steps over without ever being asked a question. The offset walks back toward the centre and the best attempt wins — over 80 seeds the hole is 5.8 to 15.6mm across and never closer than 2.6mm to the outer edge',
      'THE EDGE IS A HAIRLINE on the machine half — the only line in the section drawn at a width that is not a pen. At the nib\'s own width the outline is the heaviest mark on the sheet and takes the eye every time, which is the one thing a specimen must not do. What plots is the nib; this is the specimen\'s own annotation, and it is thin so that it says so. It is still counted, because a thin line is still a line the pen went down to draw',
      'NEW SHEET on the machine half too, re-rolling the outline and the hole. A seeded fill judged from one shape is a fill judged from nothing',
      'THE PEN WENT UP TO 0.35 and every pitch went up by the same 1.4 with it, which is a change of PEN and not of density — the nib-to-gap ratio is what a hatch looks like, and it is unchanged. 0.25 was a defensible choice and an unreadable one: at 1:1 on a 96dpi screen it is a sub-pixel line, so the fill and the hairline edge arrived at the same weight and neither could be told from the other. The document already says this about the bay — a 0.20 nib LOOKS like a sub-pixel line because that is what 0.20 is — and the fix for a line you cannot see has never been to draw it wrong, it is to pick up a bigger pen',
      'ONE SHEET PER FILL, ball and shape side by side on it with a gap, one row per fill instead of a grid of separate screens. A screen per drawing is the wrong unit: the question a row asks is what ONE fill does to two different problems, so the two answers belong on the same paper — same stock, same pen, same run — and each caption sits under the drawing it is about rather than in a shared strip you have to decode',
      'AND `.flat` WAS ALREADY TAKEN. It is the pale comparison panel in 01 and it carries a 1px WHITE border and a 16px radius; `.hh-field` overrode its background and its shadow by source order and had no reason to mention `border`, so the shape sheet came out wearing a white edge the tone sheet beside it did not have. Two rules that never met, agreeing on everything except the one property neither declared. The file already records this exact lesson — check what the stylesheet says about a name before reusing it — and the fix is not more specificity, it is not taking someone else\'s word: the class is `hh-shape`, and the two segment classes went from `.a`/`.b` to `.hh-a`/`.hh-b` for the same reason before they collided with anything',
      'THE BLOB\'S RADIUS IS DERIVED, not chosen. Its four harmonics sum to 0.65, so the widest point is 1.65 × base — pick base as a fraction of the sheet and about one seed in ten runs off the edge of it. Sized from the worst case with 2.5mm of margin, no seed can',
      'chain() takes a BRIDGE rather than a reach — a function that either returns the line the pen draws on the way, or says no. That is the whole of where the argument lives',
      'NEW SHARED BLOCK — the SPECIMEN KIT. Both halves draw fills and agree about almost nothing, but they must not disagree about THE THING BEING DRAWN: light the ball differently or put a different neck in the blob and the two stop being comparable, which is the only reason either is worth having. scene(), blob(), latitudes(), parallels() and carve() moved out, and each half owns nothing but its own hand',
      'The readout is a .lcd rather than a caption, because it is the RESULT and not a label on one — and the control column\'s is .dim, the off state that is powered rather than dark, which is exactly right for a number that is correct and is not the one being argued for',
      'THE MACHINE BALL PROVES THE HAND\'S CLAIM FOR FREE. Nothing down there is dithered, so the four thresholds show as clean BANDS — the contour map of the lighting that the hands above spend a paragraph avoiding. Both balls are correct; only one of them looks drawn',
    ],
  },
  {
    version: '1.10.0',
    date: '2026-08-15',
    changes: [
      'THE SPHERE TEST — the first block in this document that is not a control. Everything before it is a thing you touch; this is a thing you JUDGE, and the whole of it is three fills drawn twice each with nothing to set.',
      'THREE NEW HANDS: HAND HATCHING, HAND CROSS HATCHING, HAND RANDOM LINES. They are not a re-tune of the bay\'s Hand hatched position — that one breaks the register and stops, which makes a fill look NOISY, and noisy is not hand-made. What a hand does is a list, and every item on it is something a loop does not do',
      'IT DOES NOT SPACE EVENLY, and that is the one that mattered most: an unvarying pitch is a louder machine tell than straightness ever was, so every family is jittered by a third of its own spacing before any wobble is applied. Wobble alone was tried first and reads as a shaky machine',
      'IT DOES NOT COME BACK AT THE SAME ANGLE (each pass drifts a couple of degrees off the last), IT DOES NOT STOP ON THE LINE (a stroke overshoots the boundary or pulls up short, never by the same amount twice), and IT RE-GRIPS — past about 16mm the wrist runs out of travel, so a long fill contains no edge-to-edge strokes at all',
      'TONE IS BUILT BY GOING BACK OVER, not by drawing a grey: four passes, each confined to what is already darker than the last. Every threshold is dithered with noise, because a clean one draws a CONTOUR MAP of the lighting — the fill bands into visible isolines, which is the one thing that never happens by hand',
      'A FLOOD IS NOT A DARK. Four interleaved passes at pitch s put the darkest tone at s/4, so s has to stay four nibs clear of the crossing or the terminator turns into a solid black disc. Same crossing the hatch bay marks in red on its spacing knob, met again from the other side',
      'THE STROKES FOLLOW THE FORM on the ball, built as latitude circles about an axis tilted behind the screen plane — not as a rotated grid. A grid is right for a flat fill and silently wrong on a sphere: it has no idea where the surface turns',
      'THE AXIS IS AIMED DOWN THE LIGHT, which is the geometric problem this block had to solve rather than tune around. Every axis has a pole on the front of the ball and a place where its circles shrink to a point; put either in the middle of the shading and there is a visible whirlpool in the fill. They cannot be removed, only placed — at 20° behind the plane both land near 0.94R, and the light\'s own screen direction puts the highlight at 0.79R. Aim the axis down the light and the blank cap round the pole IS the highlight and the cap opposite it IS the reflected-light rim. Both are places a hand leaves paper anyway',
      'THE BALL IS THE TONE TEST because it is the one shape whose correct answer everybody already knows. Lambert alone draws a shaded DISC — what makes a sphere is the terminator being darker than the silhouette and the table\'s BOUNCE lifting the lower rim, occluded near the contact so the contact itself stays black. The cast shadow is the only thing saying "table"; there is no horizon line, because a drawn line would be a second object competing with the one being tested',
      'A SECOND SPECIMEN PER HAND, and it is not a bonus: a circle has no corner, no neck and no concave anything, so a fill can be perfect on a ball and come apart the first time it meets a real outline. The flat SHAPE carries no shading at all, one density edge to edge, and what is read there is the boundary — where the strokes stop, and whether they stop in a line',
      'COLUMNS ARE HANDS AND ROWS ARE TESTS, which is the only arrangement where both comparisons are adjacent: down a column is one hand against two problems, across a row is three hands against one',
      'EVERY MARK IS GEOMETRY IN MILLIMETRES. No filter, no blur, no fill anywhere in the block — the viewBox is measured off the element at 96/25.4 exactly as the hatch bay measures its own, so a 0.25 nib on screen is a 0.25 nib on paper and the same paths go out to an SVG and onto the plotter with no rescale. That is the only condition under which a specimen is worth looking at',
      'THE SPECIMEN GLASS KEEPS ITS REFLECTION AND LOSES ITS SCANLINES — the one place this block departs from .screen, and not a preference. The haze is a 1-in-3px horizontal rule and the thing behind it is a fill made of rules a few tenths apart; they beat, and the moire lands exactly on the property being judged. A display that adds texture to the specimen is a filter, not a display',
      'ONE PATH ELEMENT PER SPECIMEN, not one per stroke. A plotter reads movetos, not the DOM, and the random hand emits about five thousand of them',
      'Three more .eng engravings — hatching, cross hatching, random lines — each drawn as its own marks rather than as a picture of them',
      'NO CONTROLS, deliberately, beyond NEW SHEET which re-rolls every seed. Three hands are three answers to one question and what is being asked is which is right; a knob on it would invite tuning before judging. Which of the constants become parameters is recorded in FEATURES-REQUEST, not decided here',
    ],
  },
  {
    version: '1.9.0',
    date: '2026-08-15',
    changes: [
      'THE HATCH BAY — the first COMPOUND control in the language. Everything before it is one control on one row, and hatching was five of those: same shape, same green number, nothing saying which one you turn to change how the drawing looks',
      'It is built round a SPECIMEN, because the refs all put the display in among the controls that feed it — the EQ-200 draws the curve its ten faders make. You judge 0,6 by seeing how dense 0,6 is, not by reading it',
      'THE SPECIMEN DRAWS A LIT SPHERE, not a swatch. A rectangle of even hatch shows the SPACING and hides everything else — and what hatching is actually for is TONE: the same pen at the same angle making light and dark by how much paper it leaves. So it draws the subject every hatching exercise starts with, lit from the same corner as the panel, sitting on its own cast shadow. The field grew to 248px and the bay to 520px to give it room',
      'Tone comes out of WHICH PASSES REACH A POINT, not out of pressure: every scanline carries a threshold and only puts ink down where the drawing is darker than that, so light areas are reached by a few passes and dark areas by all of them. The thresholds walk the golden ratio rather than a cycle, so the passes interleave instead of banding into visible tiers — which is the one thing that gives away a machine-made hatch',
      'CROSSED now means what it means on paper: the second pass thresholds live in the top half of the range, so it only works the parts that are still not dark enough. SERPENTINE chains the runs on a pass into one path and draws the connector between them, which is also why a serpentine fill cannot make a clean highlight — the meander has to cross the light to reach the next dark. HAND HATCHED drifts its threshold and sits each pass off its own track, so no two passes agree where a tone ends',
      'A floor of 0.085 on the lit side, because at zero the highlight gets no strokes at all and the ball dissolves into the paper with no edge. A few sparse passes across the light is what a hand does and it is what makes the silhouette read',
      'The rebuild is COALESCED ON A FRAME and nothing more: a tone-driven hatch is tens of thousands of samples and a knob drag fires far more often than the screen refreshes, so the state lands immediately and the drawing is rebuilt once per frame',
      'It was briefly latched to redraw only on RELEASE, to stop the knob going sticky, and that was the wrong trade. A specimen that does not move while you turn the knob is not a specimen, it is a picture you get afterwards — the whole reason the ball is on the panel is to be watched WHILE the value changes, because that is when you can see which way it is going. Dropping frames is a cost worth paying for that; dropping the feedback is not, because the feedback IS the feature',
      'The specimen is SVG in real millimetres — the viewBox is measured off the element and converted at 96/25.4, so one user unit is one mm and what is on screen is what plots. DOM, not canvas: it takes the bay\'s ::after glass for free. That decides hatch only, NOT the gizmo or the screen — the open question at the foot of FEATURES-REQUEST stands',
      'NEW PRIMITIVE — .plate, the language\'s first floating surface. Everything else in the document is bolted into a chassis; this one positions itself against whatever summoned it, sits above the panel and goes away. Same metal as a .box with two differences, both about elevation: a harder and shorter cast shadow, and 1px of black all the way round, because a lifted edge has a dark side wherever the lamp is. Menus, tooltips and the rotary\'s popup inherit it',
      'NEW PRIMITIVE — .picker, and it replaces the one surface the OS still drew. input[type=color] renders its popup in browser chrome and no CSS reaches it, so touching a colour produced a bright system sheet on a machined panel: the exact moment every other primitive exists to avoid. Field behind the same glass as any display, hue on a real fader with the knurled cap and a scale at the six primaries, one lit .lcd window with editable R/G/B and hex instead of three white boxes, eyedropper as a .key that hides itself where the API is absent, and a well of recents built from the same .pencap the ink rail uses. Dark on purpose — a picker is the one place the panel must disappear so the colour can be judged',
      'FIX — a swatch stopped following the picker after the first colour it was given. Filling an empty slot called drawPens(), which threw away every button and built new ones, INCLUDING the element the open picker\'s callback still pointed at. Every later drag then wrote --ink to a detached node: the specimen and the well kept up because they read the data, and the swatch under the cursor froze at whatever it was when the rebuild happened. The rail never needed rebuilding — filling a slot is a class and a custom property on a button that is already correct',
      'FIX — the same handler captured `empty` as a snapshot and outlived it, so once a slot was filled the next click reopened the picker instead of selecting the pen. It reads the state now, not the photograph',
      'THE PICKER\'S WELL SHOWS THE CASE, NOT A GUESS AT ONE. It was seeded with five hard-coded colours, which is the worst of both: they look like they mean something and they mean nothing, and the moment the rail changed they were five stale duplicates of a palette that no longer existed',
      'So the CALLER says what belongs in the well. A picker opened from a rail passes that rail — the other pens in the case, the set you actually reach for, live by definition and passed as a function so it re-reads rather than freezing a copy at open time. A picker opened from nowhere in particular falls back to what has genuinely been picked before, starting EMPTY rather than starting wrong, and the well hides itself until there is something in it',
      'The swatch you are editing is SEATED in the well, exactly as it is in the rail. Two rails of the same object showing the same state the same way is the whole reason it is the same object',
      'The native input is kept as the VALUE and never as the surface, so swapping the picker in changes the caller and nothing downstream. openPicker() is a primitive, not a bay feature: the hatch bay\'s pens call it and its own Materials tile calls it, which is what makes it one',
      'NOT built, and it is the last piece: the plate does not trap focus. Escape and click-outside close it, but Tab still walks into the page behind. Recorded in FEATURES-REQUEST against .plate rather than .picker, because it decides the pattern for every floating surface after this',
      'THE THREE PARAMETERS ARE THREE KNOBS, which is what this document\'s own rule already said — "every slider that sets a quantity became a knob; a length stays a fader". Pen, spacing and angle are three single quantities and none is a span with two ends you compare, so all three were faders in defiance of a rule the page states twice. One row of 46px knobs also costs a third of the height three param strips did',
      'THE TYPE SELECTOR IS THE ROTARY, not a bar of keys: Parallel / Crossed / Hand hatched / Serpentine. A row of keys shows four labels and makes you infer they are one choice; a selector switch has one lever on one detent, so the fact that they are alternatives is in the object rather than in the reading',
      'FOUR HATCH TYPES: PARALLEL, SERPENTINE, CROSSED, HAND HATCHED. They are four different PATHS, not four textures — parallel lifts the pen at the end of every pass, serpentine does not and is one continuous stroke, crossed is a second pass at 90°, hand hatched breaks the register so the fill has no readable direction',
      'The lines are generated in world space and clipped to the sheet (Liang-Barsky) rather than drawn on an over-sized grid and rotated. That is not tidiness: a serpentine IS its turnarounds, and on an over-sized grid every turnaround falls off the paper — it would have rendered as PARALLEL under a different name on the dial. Clipped, every endpoint lands on the boundary where a real fill puts them, and SERPENTINE emits exactly one moveto where the other three emit one per pass',
      'PEN RUNS FREE, 0.1 to 3 mm. It was six detented stops on the argument that a nib is one of six objects in a case — true of a technical pen, false of this panel: what is being set here is a STROKE WIDTH, and a stroke width is a quantity like any other. A kit is the right model for choosing a pen off a shelf and the wrong one for dialling a line until it looks right, which is what a specimen in front of you is for',
      'The dot that showed the nib at true relative size went with it. At 0.1mm it is a speck and at 3mm it no longer fits a label row — a scale that only works across one octave is not a scale',
      'PEN, SPACING AND ANGLE ARE INDEPENDENT. An earlier build had the pen drag spacing along to hold a nib ratio — a tidy idea and the wrong one, because it means one control silently edits another and a value you set by hand stops being the value you set. Three settings, three knobs, no hidden wiring',
      'SPACING UNDER THE NIB IS NOT REPORTED, because it is not an error. The strokes overlap and the sheet fills solid on its own — that is a fill, and a fill is something you ask for. An earlier build lit a red legend and turned the spacing arc red, framing a legitimate result as a fault; the specimen already shows exactly what happens, which is the whole job of a specimen',
      'THE GROUND IS PAPER NOW. It was near-black, which worked for exactly as long as the ink was orange — and the case has BLACK in it, which is the pen you own most of. Black on black is nothing and grey on black is worse, because it looks like it is working. Same argument the colour picker entry makes: the surface a colour is judged against cannot compete with it',
      'THE INK RAIL IS A CASE, so it is full of SLOTS — three pens it ships with (black, grey, white) and six empty ones. A rail that stops after the pens you happen to have reads as a list that ran out; a case reads as a case, and an empty slot is an invitation rather than an absence',
      'AND EVERY SLOT IS REFILLABLE, including the three that ship full. Black, grey and white are what a case comes with, not what it is stuck with — a swatch you cannot change is a swatch lying about being a pen. The fixed/refillable split is gone and the engraved notch that marked it went too: a mark that is on everything marks nothing. What still separates a slot from a pen is whether there is anything IN it, which the well says by itself',
      'AN EMPTY SLOT IS A HOLE, not a faded pen. The wrong build is a cap at low opacity, which reads as broken or disabled — a slot with nothing in it is a piece of the CASE: a shallow milled well, shorter than a cap because no cap is standing in it, with the same two hairlines every other recess has, and no colour at all because it does not have one yet. It lights its own rim on hover rather than lifting, because a hole cannot come towards you',
      'Empty opens the picker on the FIRST click, because there is no pen to choose yet — filling it is choosing it. A filled slot selects first and refills on the second, so choosing a pen never costs a plate. The slots are spread rather than packed, so the rail ends where the panel ends however many pens are in it',
      'THE STOCK IS TONED (#BEB5A3), because the case has a WHITE pen in it. White on cream is not a subtle result, it is no result — the sheet would look broken rather than look like white ink. Toned stock is what you reach for when you plot white, and it is the one ground where black, grey and white all read at once: contrast against the sheet is 8.7 / 2.9 / 2.0, and grey separates from both its neighbours',
      'fader() exposes .set(), the handle knob() already had, so one control can move another',
      'THE INK SWATCH IS A CAP WHOSE FACE IS THE COLOUR — and it took three wrong objects to get there. A pen cap read as a battery. A nib read as a marker tip. A glass pipette read as a pipette and was still wrong, because all three were the same mistake: each got better at being a PICTURE of a thing on the desk and none got better at being a CONTROL',
      'A rail of little drawings is an illustration. You cannot tell at a glance which one is selected, the colour fights a silhouette for attention, and at 17px the object being depicted is unreadable anyway. So the swatch stopped depicting the pen: the colour gets the whole face, and the state is the same up/down every switch and piano key on the panel already uses',
      'IN USE IS IN — AND IT DOES NOT FALL. Same decision the small piano keys already made: at 28px there is no room for the drop to be legible as a drop, so the movement goes and the state stays. The recess and the chamfer say pressed; hover lights a rim rather than moving anything, the way every key on this panel changes colour under the pointer rather than position',
      'And the pressed recipe is the KEY\'S OWN, applied to a coloured face: the cap keeps its own colour and the gradient INVERTS — dark at the top, light at the bottom, because the bezel is now shading a face that has dropped below it and what light reaches it bounces up off the well. Nothing is tinted, so the swatch you press and the one held down are recognisably the same object in two positions. That is the rule the key learned three times and it did not need learning a fourth',
      'AND THE LAMP, which is the other half of latched — down AND lit is what separates "held by a finger" from "held by the mechanism". Every other latching control here says it with a warm GLYPH and a colour swatch has none: the face IS the legend, and tinting it orange would be lying about the ink. So the lamp goes into the WELL — a tight warm line at the cutout and a short bloom off it, which is light escaping round a seated cap',
      'It is the PANEL\'S lamp colour and not the ink\'s, deliberately: an indicator that changed colour with the thing it indicates stops being an indicator. Held to the source\'s own limit for a latched control — a little light in the well, not a bloom on the chassis',
      'MODELLED, NOT FLAT, which is the only thing separating it from the soft pastel swatch it is shaped like: light from above left at 158°, a hairline of light on the top edge and a dark one under it, and the specular OFF CENTRE because a centred highlight reads as a sticker',
      'Min stroke and Join reach went into a recess below the fold. They are set once per plotter; five identical rows was itself most of why the panel was sad',
      'NEW PRIMITIVE — .eng, the engraved ICON. The language had engraved TYPE and nothing engraved that is a SHAPE, so every icon was a flat monoline glyph laid on the metal: the sticker failure the file already names about a centred highlight. Stroke in the panel\'s shadow colour, one 1px light line under it, never white and never currentColor — an engraving does not change colour when its row is selected, because it is a hole. Six of them so far; every label row in the language can have one now',
      'knob() takes an optional detents array — the angle knob notches at 0 / 45 / 90 / 135 / 180. Same idea the centre fader already had: an angle of 45° is a place, not a value you approach',
      'REMOVED the SLIDER → KNOB A/B card from 04. Sections renumbered: the hatch bay is 07, Materials 08, the bill 09',
    ],
  },
  {
    version: '1.8.0',
    date: '2026-08-15',
    changes: [
      'MATERIALS stops over-claiming. The lede promised that nine primitives carry all six components and that every control on the page is one of them at a size — a completeness claim nobody re-counts when they add a tile, and it read "nine" while the grid held thirteen',
      'The count is now written by the tiles themselves. Adding an entry to the array is the only edit there is; the sentence cannot drift again',
      'Three primitives that were on the page and had no tile: READOUT (the .lcd, used 4× — a lit window cut into the case, phosphor digits, and .dim as an off state that is powered rather than dark), MICRO KEY (22px and recessed, because duplicate and delete must not invite the thumb; delete is the only red in the document and only on hover), KNURLED CHEVRON (the thing you flick — shown at its real 20px, since the knurl is tuned against an 11px glyph and enlarging it would show a texture the document does not have)',
      'Both new key tiles are live for the same reason the fader ones are: a micro key\'s whole content is its hover and a chevron\'s is its flick, so a static sample shows the half that does not matter',
      'The knob/fader rule was stated once, in a comment under .knob, and was therefore invisible from the section built to teach the primitives — a reader concluded the language\'s answer to a slider is "turn it into a knob". It is in the knob tile now: a quantity is a knob, a length stays a fader',
      'FIX — .chev had no position:relative, so the chamfer ring that .chev.open::after draws resolved against .row and was clipped away by its overflow. The one control the chamfer rule names by name was the one control not getting one',
    ],
  },
  {
    version: '1.7.0',
    date: '2026-08-15',
    changes: [
      'The fader family, from the refs: the EQ-200, the TX-6, the K.O.II and the Sidekick are all banks of ONE-cap faders, and the document only had the two-cap range one, which is the exception, not the type',
      'fader() — one factory, three forms, because they differ in two things only: where the lit fill starts and which way the slot runs. LEVEL fills from the end of the slot to the cap. CENTRE fills from the middle out to whichever side, for a value that is an offset rather than an amount. VERTICAL is the same control stood up, with the readout on top and the legend at the foot',
      'The centre form DETENTS: inside 3% of the middle the cap snaps to zero and the scale carries a full-height tick there, the way a real centre-detent pot has a notch cut in its track. Zero is a place you can find without looking',
      'All three share the range fader\'s geometry — a cap centre at t sits at t*100% - (t-.5)*17px — so a cap stays inside its slot at both ends without the value lying about where it is. Track takes every pointer event and the cap comes to the click; wheel and arrows work, shift for fine',
      'Three material tiles for them: FADER · ONE CAP, FADER · CENTRE DETENT, FADER · VERTICAL BANK (three of them, since the reason vertical exists is that a bank of caps makes a profile you read across)',
    ],
  },
  {
    version: '1.6.1',
    date: '2026-08-15',
    changes: [
      'The bright skin came back in the 1.6.0 commit and is removed again: 296 lines of [data-skin="light"], the .skin-switch rules, the empty switch element (its script had already gone, so the page was rendering a stray fixed well top-right) and the restored skin() IIFE that sat directly under the comment explaining why it was gone',
      'MATERIALS was missing the fader, the one control the lede promises by name: every slider that set a quantity became a knob, except a length, which stays a fader. It is there now, live, built by the editor\'s own rangeFader with the same numbers',
      'Its tile spans two columns. A fader sampled in a 232px column leaves the track about 90px once the label and the readout are in, which is not a length, it is a stub. Tiles take an optional class as a fourth field for this',
      'NOTE: physical-ui.css.bak is tracked and still contains the whole bright skin. It is a 1750-line copy of the pre-rename build; left alone here because another agent may be relying on it, but it is where the light mode will come back from next',
    ],
  },
  {
    version: '1.6.0',
    date: '2026-08-15',
    changes: [
      'The bright skin is removed — 336 lines of [data-skin="light"] overrides, the DARK / LIGHT switch and its script. Every effect in this document is light modelling against a dark body: a cap is lighter than its case, a well is darker, and a recess is made by taking light away. That cannot be inverted, only rewritten, so the pale build was a second set of hand-tuned numbers drifting from the first every time either was touched. One material, one set of numbers',
      'The exploration keeps its external stylesheet: physical-ui.css stays a file, dark only',
      'The piano wells (page switcher and editor tabs) get the floor the bay was given back at 1.4 and never shared: #17191d → #1e2126 instead of near-black. A key\'s skirt is #0d0e10, so on a #0a0b0d floor the one part that proves a cap has a body was invisible and the key read as a grey rectangle printed on black. Inset shadow softened to .8 to match',
      'The language has a name: SKEW. It is the word the hand types when it means skeuomorphic, and it is also the trick itself — a flat plane tilted into depth, lit from one lamp above left, nothing actually raised. In the page as targz-skew-design-system',
      'The MOMENTARY VS LATCHING tile was two spans wearing the key classes, under a caption promising that one springs back and the other holds: a sample that could not do the thing it was a sample of. They come from the same key factory as the nav bar now, and both work',
      'Its glyphs were 24px in a 38px cap, leaving 7px of face — on the lit one the sun\'s rays ran into the walls and the orange stopped reading as a legend and became the button\'s colour. 18px now, the nav bar\'s 16-in-34 ratio at this size',
      'The MILLED WELL material tile was demonstrating the fault it documents: the tile stage was #101114, darker than the well cut into it, so the sample was a lighter rectangle on a dark ground. The stage is case metal now, the well sample carries the real recipe, and the caption states the floor rule',
    ],
  },
  {
    version: '1.5.1',
    date: '2026-08-15',
    changes: [
      'Tool palette: keys were 5px apart, which is not 5px of daylight — each raised cap lays a 3px hard skirt plus its blur underneath, and a pressed cap drops 2px further into the gap below it. Pan / Turn / Select were touching. Now 10px, in line with the keypad\'s 11px between stacked rows, and the palette padding goes 7 → 8px so the outer margin does not read as tighter than the inner one',
      'TOOL caption sits 6px above the first key rather than 4px, so the label is not closer to the key than the keys are to each other',
      'Editor tab strip had the same fault worse: 3px between tabs, each laying a 2px skirt, so Letters / Place / Size / Drop / Shade were one object with lines drawn on it. 7px now, and the strip padding 4 → 5px',
      'The editor tab no longer travels. A 26px cap falling into a strip this tight puts its shadow on the neighbour, and there is no room for the fall to read as a fall — it still goes in (recess, chamfer, lamp), it just does not move',
    ],
  },
  {
    version: '1.5.0',
    date: '2026-08-15',
    changes: [
      'Physical UI exploration: the pressed recipe is now two tokens in :root — --recess (the four sharp walls plus the lit lip) and --chamfer (the directional, concave-polarity ring). Five copies of it across switches, keys, tabs, chips and chevrons was five things that drift apart the next time one is tuned',
      'Every toggle in the document now uses it: layer switches, stroke switches, the ALL masters, nav bar latches, editor tabs, the page switcher, view keys, folder chips and chevrons. None declares a fill — each inherits its own raised face, so a pressed cap stays the same object as the cap beside it',
      'Travel normalised: 2px on the big keys, 1px on the small. A tab falling 4px was travelling further than the gap to its neighbour',
      'Fixed: .keypad .pkey and .piano.sm .pkey re-declared box-shadow at the same specificity as the pressed rule but later in the file, so the view keys and editor tabs kept their RAISED shadow when down and only the label changed colour. The size overrides are scoped to :not(.is-down):not(:active) now',
      'Fixed: .chev.open\'s box-shadow ended in a stray comma, making the whole declaration invalid — the open chevron had had no shadow at all',
      'Toggles get the momentary key\'s actuation surge when they engage, and only when they engage: flipping a switch off is a circuit opening, there is nothing to surge. Reuses the same keyframe rather than a second one, since two nearly-identical surges read as one done badly',
      'Pan / Turn / Select moved out of the viewport into their own tool palette. Everything else in that panel answers where the camera is; those three answer what the mouse does, which stays true while you are not looking at the viewport at all',
      'Focus: removed eleven accent outlines and answered :focus-visible explicitly — deleting them had only uncovered Chrome\'s own white-blue default. One hairline everywhere, 1px in the panel\'s orange, plus a brightness lift on the control itself',
      'Removed the colour dot from numbered layer rows; the ALL row keeps its jewel, which wears the whole palette',
    ],
  },
  {
    version: '1.4.2',
    date: '2026-08-15',
    changes: [
      'Physical UI exploration: the chamfer round a pressed switch is now a ring with a direction, not a hairline. A cutout edge is broken all the way round and is not equally lit anywhere — it flares where it faces the lamp and dies as it turns away — and a box-shadow, being the same value the whole way round, always reads as a drawn outline instead',
      'Corrected its polarity, which was the actual bug: the ring was bright top-left and dark bottom-right, which is a DOME. A chamfer diving into a hole is the opposite — its upper edge tilts away from the light and sits in shadow, its lower edge tilts toward it and catches. It was also arguing with the walls inside the cap, which were already right, and a bevel saying "out" wrapped around a recess saying "in" resolves as a raised button every time',
      'Pressed shadow +20% in geometry (offsets and blur, alphas untouched), and the pressed legend is back to full size',
      'Removed the colour dot from numbered layer rows — the light pipe on the leading edge is the same fact, said better and 6px away. The ALL row keeps its jewel, which wears the whole palette',
    ],
  },
  {
    version: '1.4.1',
    date: '2026-08-15',
    changes: [
      'Physical UI exploration: the pressed switch rebuilt. It declares no fill of its own — it inherits the raised cap\'s, so up and down cannot drift apart, and the recess is drawn entirely by inset walls. Every earlier attempt at a separate pressed colour was drawing shade with paint, and a darker cap has nothing left for a shadow to take away',
      'Raised the cap into real grey. Shadow is a difference, and a near-black face left the recess nowhere to fall — the change that makes the pressed state read is a change to the unpressed one',
      'Shadow on all four walls, not just the top: a hole has sides, and at 24px the sides are most of what you see of it',
      'Sharpened it — blur radii 6-12px down to 1-4px. A wall two pixels from the face cannot throw a shade that has softened over that distance; 12px of blur at full black is a cap sitting in fog',
      'Travel down to 1px. Two was a third of the row gap, enough that toggling looked like the row twitched',
      'The legend scales to 0.9 when the cap is down — a face further from the eye subtends a smaller angle. Too small to notice as a change, which is the point',
      'One legend per switch: the plain drawing at normal weight, never struck through or greyed. The cap position already says on or off, and a slashed grey icon reads as disabled',
      'MIXED is now the pressed state with a WHITE legend, not an intermediate height. A master whose column is half on is not half-pressed — the thing it commands is engaged, just not everywhere. Down means it is happening; the colour says how completely, and white against orange is a brightness difference that survives with no colour vision',
      'Brightened the bay, the stroke strip and the row faces to match. A recess is the case seen deeper, not a hole into space — darker than the surface, not black, with the inset shadow doing the work',
      'Rotary selector: removed the detent tick marks entirely. The tip already says where the switch is, and a second mark a few pixels away is another thing to read at the moment you are reading the tip',
      'Knobs and the selector now turn clockwise on a downward drag, and the wheel follows; arrow keys stay conventional so the ARIA slider contract holds',
    ],
  },
  {
    version: '1.4.0',
    date: '2026-08-15',
    changes: [
      'New document explorations/physical-ui-layers.html — a "what if the language were hardware" study. It is an EXPLORATION, not part of the system: index.html is untouched and none of these tokens exist there',
      'Six shipping components redrawn in the skeuomorphic register, each with its real anatomy: the Portrait-Typo layer dock (lifted control-for-control from its ui.js), the nav bar, the page switcher, the layer editor, the viewport and a Launchpad project card',
      'Buttons are objects with a body: a skirt, a cast shadow and real travel. Momentary keys spring back and pulse once; latching keys hold the same position a finger would, which is the distinction flat UI has no way to make',
      'Latched, pressed and open all share one recipe — the cap keeps its own colour, the gradient inverts, a hard black inset runs along the top and a hairline of light along the bottom, and only the legend lights. Three-state switches sit at three physical heights (up / half / down), so the state reads in greyscale',
      'One legend per switch: the plain drawing at normal weight, never struck through or dimmed, since the cap position already says on or off and a slashed grey icon reads as disabled',
      'Sliders that set a quantity became knobs — 270° of travel, an SVG-stroked value arc (a conic gradient behind a CSS mask aliases on three edges), a pointer, drag/wheel/keys, and a bipolar mode that grows from 12 o\'clock. Drag down turns clockwise',
      'Zone from / Zone to became ONE range fader, because the value is one length: cut slot, engraved scale, two waisted caps that cannot cross, a hatched band between them',
      'The Place-from dropdown became a rotary selector: a pale teardrop lever, positions fanned into 76° with their names as a vertical list, leader lines absorbing the angle mismatch',
      'The editor panel bulges around that selector — rounded rectangle unioned with a lobe and joined by tangent fillets, as one clip-path on one element, so the fill cannot seam. Its drop shadow is a filter on a wrapper, because a box-shadow is cast by the border-box and would cut the bulge in half',
      'Glow is spent only on things that emit: light pipes, LED readouts and the big illuminated action keys',
      'Closes with the bill — what the language buys, and what it costs (dark-only, paint cost per row, small type against grain, and that it out-shouts the drawing)',
    ],
  },
  {
    version: '1.3.0',
    date: '2026-08-06',
    changes: [
      'Canvas, grid & rulers (section 03): replaced the static "warm grid" description with how it actually works — the grid is derived from the sheet, not a fixed 40px CSS pattern',
      'Documented the division formula round(max(canvasW, canvasH) / 60) clamped to 4-30, so grid lines always land on the paper edges and continue to the window edges',
      'Documented the size readout: bottom and right rulers pinned to the sheet, a tick per division but only the two ends labelled, in millimetres',
      'Recorded that the Y axis is inverted (paperH - mm) so the origin is bottom-left, matching the plotter bed',
      'Added a demo showing an A1 square sheet at 594 x 594 mm with its grid and both rulers',
    ],
  },
  {
    version: '1.2.0',
    date: '2026-08-06',
    changes: [
      'Side panel (section 06): rewritten as a Tweakpane 4 spec — the hand-drawn panels were never Tweakpane, which is what every app actually ships. Adds a prominent "do not hand-build this" callout, a faithful replica of a skinned pane, a widget anatomy table mapped to the --tp-* variables, placement rules, and the full skin block to copy',
      'Nav bar (section 07): replaced the old Bottom Dock with the glass nav bar as actually shipped — 40px items, popover menus with kbd shortcuts, separator, status readout — plus a full spec list',
      'Logs (section 10): the log is now an event, not a surface — silent by default, a transient toast above the nav bar, and an on-demand history in a nav bar popover, with repeat collapsing, dwell rules and an unread dot',
      'Tab bar (section 11): added the white pill page switcher — demo, spec and states. Active is an --tg-orange-soft fill with an --tg-orange label; focus is a 3px orange halo, never the browser default',
      'Section 11 renamed to "Tab bar & app header"; the docked app header is kept for windowed tools',
      'Console is now bright — white surface, hairline border, orange $ prompt — instead of the black block',
      'Scoped the console to streamed output from external processes (vpype, plotter drivers)',
      'Added a contents rail with scroll-spy, built from the sections at runtime',
      'Added glass tokens: --tg-glass, --tg-glass-border, --tg-glass-hover, --tg-shadow-float, --tg-shadow-pop',
      'Everything else in the document is unchanged',
    ],
  },
  {
    version: '1.1.0',
    date: '2026-05-01',
    changes: [
      'New design system created with Claude design (targz Design System.html)',
      'Remove previous design-system.html (no longer relevant)',
    ],
  },
];
